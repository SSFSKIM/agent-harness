---
status: archived
last_verified: 2026-06-12
owner: imprint-job
type: session-digest
tags: [tech-debt, recursion-guard, persona-dispatch, documentation]
description: Session digest covering the fix of tech-debt item m4, correcting the recursion-guard contract docs to match the exact-string check in harness_lib.is_headless() and exercising in-plugin persona dispatch.
---
# Session digest — 2026-06-12 · 181a7ab9 · session-end

## What was attempted

Fix tech-debt item m4: `knowledge/recursion-guard.md` stated the recursion-guard contract
imprecisely ("returns True if `HARNESS_HEADLESS` env var is **set**") when the actual
implementation (`harness_lib.is_headless()`) uses an exact-string check `== "1"`.

After the fix: commit, run `check.py`, then dispatch the `agent-harness:review-arch` persona
to verify the diff — a live exercise of in-plugin persona dispatch via the Agent tool.

## What changed

| File | Change |
|---|---|
| `docs/memory/knowledge/recursion-guard.md` | Tightened `is_headless()` description: now states `HARNESS_HEADLESS == "1"` (exact string match); notes that `"true"`, `"yes"`, etc. do NOT trip the guard. |
| `docs/exec-plans/tech-debt-tracker.md` | Marked m4 row `fixed`. |

**Commit:** `28382ef` — "Fix recursion-guard contract wording (tech-debt m4)"
**check.py:** GREEN (36 tests, lint_structure OK, lint_docs OK, gen_inventory OK)

## What was learned

- `agent-harness:review-arch` persona is reachable via the `Agent` tool with
  `subagent_type: "agent-harness:review-arch"`. No TaskCreate step needed; the Agent tool
  works directly.
- The persona correctly took a fast-path for a docs-only diff (no layer-law, no logic, no
  cross-cutting concerns to check). Verdict: **SATISFIED**, no P1/P2.
- Persona proposed a traceability rule (not blocking): tech-debt tracker rows should cite
  the implementing commit SHA when moved to `fixed`, to prevent premature closure.
  → Logged as open question `openq/tracker-fixed-traceability.md`.

## What is unfinished

- M6 (Task 17): exercise the dream skill dispatch path via the Task tool end-to-end.
  Still pending — this session did not address it (orthogonal to the tech-debt task).
