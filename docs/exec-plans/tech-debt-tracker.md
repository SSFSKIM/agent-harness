---
status: stable
last_verified: 2026-06-26
owner: doc-gardener
type: tracker
tags: [tech-debt, fix-forward, doc-gardener]
description: The running tracker where fix-forward findings from gate P2s and gardening passes land for continuous GC by the doc-gardener.
---
# Tech debt tracker

Fix-forward findings land here (gate P2s, gardening findings). doc-gardener
GCs continuously — debt is a high-interest loan.

**Source convention:** link the originating plan/spec in the **Source** column (a
`.md` link to the relevant `completed/…` plan or `product-specs/…` spec) so
`nav.py followups` can group debt by the node it derives from, and `nav.py map`
can badge that node. A row with no source link falls into the `(unsourced)` group.

| Item | Severity | Found | Source | Status |
|---|---|---|---|---|
| **Packaging maturity test (clean-repo bootstrap dogfood) — 1 headline gap fixed + 3 enforcement-coverage findings.** Bootstrapped the harness onto two scratch hosts (a blank `git init` repo and an existing Node app with its own `CLAUDE.md`/docs). **The deterministic half is mature:** `scaffold.py` ran clean on both (24 seed CREATEs, idempotent SKIP on re-run, the documented hook-REWRITE + inventory-GEN aside), `check.py` was **GREEN on a fresh scaffold before any judgment work** (the packaging promise holds), the existing host's `CLAUDE.md` was correctly preserved (SKIP), the `.claude`-blanket-ignore NOTE fired, and the host's ungoverned `docs/notes.md` was correctly host-owned (relaxed governance). Findings: **(D1, FIXED this pass — was Important):** the porting handoff (`harness-init` SKILL step 9) gave a new host only the *session-only* `claude --plugin-dir` flag — no persistent plugin wiring — so a session that forgets the flag loses every harness skill referenced by the scaffolded `AGENTS.md` (`docs-nav`/`execplan`/`product-design`/`harness-lint`) **silently**, while the gate stays GREEN because the pre-commit hook calls `check.py` by absolute path. The self-host loads the plugin persistently via `.claude/settings.json` (`enabledPlugins` + `extraKnownMarketplaces`) but never propagated that to ported hosts (core-belief 13). Fixed by rewriting step 9 to wire a durable form (published `/plugin marketplace add` + install, OR a `directory`-source `.claude/settings.json` mirroring self-host with an absolute path), `--plugin-dir` demoted to the ephemeral fallback. **(D2, Minor):** a hollow host with **13 surviving `FILL` markers across 6 files commits GREEN** — `check.py` never checks for `FILL`; step 8's manual grep is the *only* guard, so a `scaffold → commit` adopter who skips it lands a hollow harness. Candidate: a non-blocking `lint_docs` WARN (or check.py surfacing a marker count) — keep it a WARN to respect minimal-gates + strict-mode migration waves. **(D3, Minor):** step 6's "**Always** create a `verify` skill" is unenforced — no lint requires `.claude/skills/verify`, host finishes GREEN without it; either soften the "Always" or add an advisory check. **(D4, Minor):** `--root` contract is inconsistent — `scaffold.py`/`check.py` accept `--root`, `nav.py`/`gen_inventory.py` do not (cwd/env only), so the natural "drive nav.py with `--root <host>` like the skill drives check.py" fails; add `--root` for a uniform contract or document the cwd-only resolution. **(D5, consider):** mechanize D1's propagation — `scaffold.py` could write the host `.claude/settings.local.json` (machine-local, like the hook) so the first developer is wired with zero manual steps; deferred as a taste call (committed-vs-machine-local path; published-vs-local-checkout source). | Important | 2026-06-25 packaging maturity test (clean-repo bootstrap dogfood) | (unsourced) | D1 FIXED (`harness-init` SKILL step 9 — persistent-install handoff); **D2 FIXED 2026-06-26** (new advisory **`WARN D13`** in `lint_docs` — a fresh scaffold stays GREEN but the gate now surfaces surviving `<!-- FILL: -->` markers in `docs/`/`AGENTS.md`/`ARCHITECTURE.md`; matches the colon-bearing opener so prose mentions don't false-fire; KNOWLEDGE_FORMAT D-rule map + harness-init step 8 updated; 4 tests); **D3 DECLINED** (human call 2026-06-26 — verify-skill enforcement not wanted); D4 (`--root` contract) + D5 (scaffold writes host plugin wiring) tracked for the doc-gardener |
| **`needs_human` → reply-continue → `done`+PR strands the open PR (never enqueued for the merger).** The decider maps a worker's `report_outcome(status="needs_human")` to an `escalate` disposition (`orchestrator.py:230`) → the ticket is marked `escalated`/`started` (`:236-238`). When the Director answers that turnReview with `{kind: reply}` (continue) and the worker then finishes with `report_outcome(status="done", pr_url=…)`, the orchestrator posts **no terminal turnReview for the final done** and **never runs `_maybe_enqueue_merge`** — so the ticket stays `escalated` with a clean open PR that never auto-lands. Observed live on the 2026-06-25 codex dogfood (LIN-30 → PR #4): queue had exactly one turnReview (the needs_human), `run.stopped_reason=pass_complete`, `recent=[(LIN-30, escalated)]`, but `last_message` was the done+PR message — a status/outcome mismatch. Worked around by manually `dq.append_merge_request(...)` + `merger --once --mock` (the runbook §11 documents the workaround). Fix: after a reply-continue to a `needs_human`/escalate turn, the worker's subsequent terminal `done` must post a terminal turnReview / flow into the merge-enqueue path (don't let the earlier escalate marking shadow the final done). Root-cause + fix is a separate orchestrator plan. | Important | 2026-06-25 codex runbook dogfood (LIN-30) | [director-runbook-decouple](completed/2026-06-25-director-runbook-decouple.md) | open |
| **`director.run` single-ticket path is un-observable — the safest run path is the only blind one.** The per-ticket session-event stream + `director.dashboard`'s `/api/v1/ticket/<id>/(events\|stream)` route (the `3ca4062` observability work) are wired ONLY into the orchestrator: `orchestrator.py:475` passes `on_event=lambda ev_: self._observe_event(tid, ev_)` → `self.events.record(...)`. But `director.run.main` — the single-ticket `--linear <ID>` path that the runbook `docs/DIRECTOR_RUNBOOK.md` §5 (formerly `.claude/DIRECTOR.md` §0) recommends as the *safe* way to run one ticket — calls `drive(...)` with no `on_event` (`run.py:571`, defaults to `None`), so it emits zero ticket events AND writes no orchestrator status/queue, so `director.watch`/`director.dashboard` (incl. the new ticket-events route) show nothing for it. Confirmed during the LIN-29 worker-policy dogfood: the only way to watch the run live was to tail the worker log with a Monitor. Fix options: thread a `TicketEventWriter`-backed `on_event` (+ a minimal status write) through `run.main` so a single-ticket run is dashboard-watchable like an orchestrator-dispatched one; or document single-ticket runs as log-tail-only and surface the log path in the run banner. | Important | 2026-06-25 | worker-policy dogfood (LIN-29, single-ticket `director.run`) | open |
| **Codex-companion review loop is single-locked and has no dead-holder detection — one wedged job blocks ALL subsequent codex reviews.** The always-on QA leans on a model-diverse (Codex) pass per `CLAUDE.md`; the codex-companion serializes resume jobs behind a lock. During the ADR 0005 (worker-methodology-surface) round-2 re-review, a resume job (`task-mqselj6e-vqgygf`) wedged — its process died but the lock was never released, so the job sat frozen ~2h51m and every later codex dispatch was *refused* (read as "a resume is in progress") with no stale-lock break and no dead-PID detection. Per `CLAUDE.md` ("if codex fails … run with Claude models") it is non-blocking in practice — the gate fell back to deterministic self-verification + a fresh-context arch reviewer — but the model-diversity backstop the QA loop relies on can be **silently** removed by a single stuck job until an operator manually clears it (`/codex:cancel`). Codex-companion is third-party plugin state this repo doesn't own, so this is a documented operating landmine + workaround, not a code fix we ship: recognize the symptom (codex dispatch refused / "resume in progress" with no live job), clear via `/codex:cancel`, and fall back to a real-code Claude reviewer for the gate. | Minor | 2026-06-25 | worker-methodology-surface (ADR 0005) round-2 re-review (wedged codex resume job) | open (operating landmine + workaround; codex-companion is third-party state) |
| **Review lens — when a cross-cutting prompt/rule changes, grep every sibling it now governs.** ADR 0004 changed `WORKER_PROTOCOL`'s "do not split a ticket by stage" rule, but the ADR/plan Consequences named only `_SPEC_/_DESIGN_TEMPLATE`; the always-on spec-compliance review (and the other two Claude personas) verified the EDITED templates and missed that the UNCHANGED `_PLANNING_TEMPLATE` now contradicted the new rule — only the Codex/model-diversity pass caught it. Proposed `docs/DESIGN.md` (Reviews) line: when a diff changes a cross-cutting rule/preamble, the spec-compliance lens must grep EVERY sibling the rule now governs, not only the files the diff touched — the prompt-policy analogue of the existing "rename = grep the surviving bodies" rule. Promote to a written rule (and consider a lint) on a second occurrence. | Minor | 2026-06-25 ticket-purpose-unit round-1 (codex caught the planning P1) | [ticket-purpose-unit-worker-policy](completed/2026-06-25-ticket-purpose-unit-worker-policy.md) | **promoted → `docs/DESIGN.md` (Agents) 2026-06-25** — recurred at the [worker-methodology-surface](completed/2026-06-25-worker-methodology-surface.md) (ADR 0005) gate, where the same green-Claude-personas-SATISFIED-but-Codex-caught-the-semantic-regression pattern hit a 3rd time (the fold silently dropped the recording obligations); the DESIGN line now codifies grep-every-governed-sibling + a model-diverse review pass for cross-cutting prompt/contract changes. Lint still deferred (recurrence-gated). |
| **Translator intra-message text/tool ordering.** `translator.ts onMessage` extracts all text then all tool_use, emitting text-before-tools; for the rare in-message `tool_use,text` order (the SDK emits text-before-tool in practice) the event stream wouldn't reflect true order. Fix = single in-order pass over `message.content` preserving the held-final-text dedup. | P2 | 2026-06-24 code-quality (codex) | [per-ticket-session-event-stream](completed/2026-06-24-per-ticket-session-event-stream.md) | open |
| **Soft-cap sentinel repeats once per process restart.** `TicketEventWriter`: after a restart, `_count_existing` re-seeds `_seq` above a capped file while `_truncated` is empty, so the next event writes a 2nd `truncated` sentinel (one extra per restart, then quiescent). Persist/skip-if-last-line-is-truncated to make it truly one-time. | P2 | 2026-06-24 code-quality (codex) | [per-ticket-session-event-stream](completed/2026-06-24-per-ticket-session-event-stream.md) | open |
| **`TicketEventWriter.last_error` is a process-wide scalar.** Under saturated multi-ticket dispatch a write failure on ticket A can be overwritten by B's, losing a diagnostic. A per-sid `last_error` dict would make it usable when most needed (file-integrity single-writer is unaffected). | P2 | 2026-06-24 review-arch | [per-ticket-session-event-stream](completed/2026-06-24-per-ticket-session-event-stream.md) | open |
| **`_count_existing` full-file slurp at first-touch.** Seeds `seq` by `read_bytes()` of the whole ticket log once per fresh process per ticket (bounded ~10MB by the 5000-line soft cap). Could read incrementally if the cap grows. | P2 | 2026-06-24 review-reliability | [per-ticket-session-event-stream](completed/2026-06-24-per-ticket-session-event-stream.md) | open |
| **SECURITY.md lacks a "GET route → request-derived filesystem path" rule (proposed T16); + a `director/` lock-free-bookkeeping invariant.** The new `/api/v1/ticket/<id>/(events\|stream)` sanitizes-before-join correctly (mirrors T14 workspace-key, T12 request_id), but T12 only says "GET reads are unfenced" and doesn't cover a GET that derives a path from a request segment — doc-completeness only, code meets the bar. arch+reliability also propose a written invariant: "lock-free per-key bookkeeping (e.g. `TicketEventWriter._seq`/`_truncated`) is sound only when each key is single-writer AND the dict tolerates concurrent distinct-key inserts (GIL-atomic on 3.12; a free-threaded runtime would need partitioning/locking)" — the no-marshal basis this diff relies on. | P2 | 2026-06-24 review-security/arch/reliability | [per-ticket-session-event-stream](completed/2026-06-24-per-ticket-session-event-stream.md) | open |
| **Claude-worker observability dogfood (classifier-only daemon, driven end-to-end through the operator console).** First real `--worker claude` daemon run, observed/answered entirely via `director.dashboard`. **O3 (FIXED, `41e97e7`) — the F3 sequel:** the F3-tuned `read_timeout_s=180s` is still too short for the claude SDK worker — its turns go silent >180s during a long tool run (a full `check.py`) between streamed messages, so `drive()` declared a *succeeding* worker `failed/turns:0`, gave up, and the worker kept running, **landed its PR, and burned ~680k tokens the orchestrator never recorded** (the dashboard + history showed `0 tok` — cost-blind). Fix mirrors `worker_runtime_sandbox`: per-runtime `director.worker_runtime_read_timeout {name: seconds}` + `resolve_worker_read_timeout(cfg, worker)` (override → else global), wired through both drive paths; runner set `{claude: 1200}` → the re-run completed, console-`done` → merger land-lane squash-merged → LIN-28 Done. **O1 (tracked):** Layer-2 live mid-turn token accrual is a no-op for claude — the adapter (`worker-runtime/app-server/src/translator.ts` `finalize()`) emits `thread/tokenUsage/updated` only at turn-end, not per-event like codex, so tokens step at turn boundaries rather than climbing live (totals still correct at the boundary). **O2 (tracked):** the rate-limit gauge (R6) renders nothing for claude — the adapter emits no `rate_limits` payload (graceful degrade, no crash, but zero throttle-headroom visibility). **O4 (minor):** `session_id` stays null for the claude runtime (the orchestrator extracts it from a codex-shaped event the adapter doesn't send) — cosmetic, but contributes to the misleading "did nothing" picture on a timeout. **O5 (minor):** after the merger's land-lane worker squash-merged and LIN-28 reached Done, its `mergeRequest` + land-lane `turnReview` were left as un-consumed pending queue entries (the merger finalized via live PR re-verify, not by consuming them) — a queue-hygiene leftover. **O1 + O3-root NOW FIXED at the adapter (`3fb7474`):** `worker-runtime/app-server/handlers.startUsageHeartbeat` — a per-turn timer (default 30s, < the Director's 180s default read_timeout) reads `session.usage()` (a control-plane query, safe mid-turn like getContextUsage/interrupt) and emits `thread/tokenUsage/updated` via the new `translator.tokenUsage()`. Gives (1) LIVE mid-turn accrual on the dashboard AND (2) a keep-alive so a silent tool run < heartbeat never trips the read loop → the per-runtime read_timeout override (`41e97e7`) is now belt-and-suspenders. app-server unit 51 GREEN; **live-verified**: a worker running `sleep 80` under read_timeout=35s COMPLETED via 3 mid-turn heartbeats (34.6/64.5/94.2s) instead of ReadTimeout-failing at 35s. **Still tracked: O2** (rate-limit gauge blank — needs the adapter to surface the SDK's rate-limit-header headroom, a separate data source the SDK doesn't currently expose to the turn stream), **O4** (null session_id), **O5** (merger queue-leftover). | Important | 2026-06-24 | claude-worker observability dogfood (live, classifier-only daemon) | O3 + O1 FIXED (`41e97e7` Director read-timeout override + `3fb7474` adapter usage-heartbeat, live-verified); O2/O4/O5 tracked |
| **Worker-methodology-delivery (phase 2) completion gate — 2 P2s fixed inline + 1 deferral + 2 proposed rules.** All 4 reviews SATISFIED (0 P1; review-reliability clean — symlink-refusal/idempotency/PR-hygiene verified preserved for the new `agents/` dir). **Fixed inline** (incomplete-rename surviving bodies — the DESIGN.md "retire/rename = grep the surviving bodies, not just the links" rule): `director/config.py:84` DEFAULTS comment + `director/orchestrator.py:1183` `--install-skills` help still said "skills (.codex/skills + .claude/skills)", omitting the now-delivered `agents/` dir; and `director/run.py` `_SKILL_ROOTS`→`_WORKER_ROOTS` (the constant governs both `skills/` and `agents/` roots now). **Deferral (review-arch — the substantive future work):** the vendored methodology skills/agents reference agent-harness repo docs (`docs/PLANS.md`, `ARCHITECTURE.md`, `docs/DESIGN.md`) BY REPO PATH, so they only resolve when the worker workspace is an agent-harness clone (the dogfood); a worker on an arbitrary PRODUCTION host repo needs the grounding docs vendored too (or the skills made path-free) for full self-containment. Recorded in the plan Assumptions/Decision log + the DIRECTOR.md bullet + the execplan SKILL.md note; not yet built. **Proposed rule (review-arch):** a DESIGN.md (Agents) line — "a persona delivered at two registry scopes (plugin-namespaced + vendored bare-name) must document both `subagent_type` forms at the dispatch site; the worker selects by runtime exposure" (already satisfied by the SKILL.md edit; codify). **Proposed rule (review-reliability):** a RELIABILITY line generalizing the PR-hygiene exclude — "Director-injected resources must stay out of the worker PR on EVERY git layout (clone AND worktree); resolve the real gitdir (`git rev-parse --git-dir`) rather than assuming `.git/` is a directory" — PRE-EXISTING (the `_exclude_injected_skills` `is_dir()` guard predates this diff; the worktree-no-hook gap is already tracked below) and host-config-dependent (the default worker workspace is a full clone), so not a regression. **Post-completion codex 3rd-person pass** (model-diversity corroboration) then found 3 install-hardening gaps ALL FOUR Opus personas had missed, ALL now FIXED + regression-tested in a follow-up: (a) HIGH — `_exclude_injected_methodology` wrote `.git/info/exclude` via symlink-following `is_dir()`/`mkdir`/`write_text`, so a sandboxed prior worker could symlink it to escape the workspace (contradicting the installer's own symlink-refusal); now refuses symlinked `.git`/`info`/`exclude`. (b) MEDIUM — the unlink-before-copy missed special nodes (fifo/socket/device) → a planted node broke the idempotent re-run; now removes any non-dir node. (c) LOW — two sources sharing `skills/` could silently clobber on a future name collision; now asserts disjoint `(subdir, name)` and fails loud. | Minor | 2026-06-23 | [worker-methodology-delivery gate](completed/2026-06-23-worker-methodology-delivery.md) | open (phase-2 P2s fixed inline; codex install-hardening fixed + tested; production-host docs-vendoring deferral + 2 proposed rules tracked for the doc-gardener) |
| **Workspace-skills-plugin (phase 1) completion gate — 1 P2 + 2 convergent proposed rules + a latent.** All 4 reviews SATISFIED (0 P1; review-reliability clean — the six install safety contracts verified preserved). (1) **P2 (review-code-quality):** `EXPECTED_SKILLS` is duplicated knowledge across `tests/test_workspace_plugin.py`, the `director/run.py` install loop, and the `NOTICE`/marketplace skill lists — a known coupling; the test is the governance lock so pinning the exact set is intentional. Single-source only if the worker-skill set churns. (2) **Proposed (3 reviewers convergent — spec-compliance + arch + reliability):** a DESIGN.md carve-out to the "retire = grep the surviving bodies" Skills rule distinguishing a **live wiring pointer** (code / tests / `.claude/` operating config — must repoint on a rename) from a **dated design-record mention** (`adr/`, dated `product-specs/`, `completed/*` — keep as-of-then). This plan exercised the carve-out (left ~10 `director/workspace_skills/` mentions in dated product-specs) but the rule as-written names only links, so reviewers read the surviving prose as a near-violation. (3) **Proposed (review-arch + review-reliability):** an ARCHITECTURE.md "Two layers" note naming a vendored, separately-licensed bundle (`plugin-workspace/`, Apache-2.0) as a **third top-level category** governed by its own test, outside the single-`hl.plugin_root()` S/D gate lints — so the next reviewer has a written authority rather than re-deriving Approach B. (4) **Latent (review-code-quality):** `hl.read_frontmatter` collapses a multi-line YAML **block-scalar** `description:` to `""` (the vendored Symphony skills use that form) — a `docs/` page authoring a block-scalar description would falsely trip D11. Out of scope here (worked around in the test). | Minor | 2026-06-22 | [workspace-skills-plugin gate](completed/2026-06-22-workspace-skills-plugin.md) | open (doc-debt) |
| **Packaging Slice 6 (completion gate, capstone) — 4 proposed rules + a taste note.** All five personas SATISFIED (one in-gate R22 fix to `lint_base`). Tracked: (1) **proposed (review-arch):** write into DESIGN.md the **script→script-import prohibition** — `lint_structure.ALLOWED_IMPORTS` mechanically enforces "scripts import only stdlib + `harness_lib`" but no prose rule states it (this slice promoted `SEEDS`/`render`/`components_table` to `harness_lib` precisely to avoid `lint_base` importing `scaffold`); plus a one-line **`base/` artifact discipline** (rendered-not-baked, markers-preserved, self-host-gated drift-check, kept out of `docs/` so repo lints/nav don't scan it). (2) **proposed (review-spec-compliance):** amend the parent spec's R6.4 to name "the self-host **instance** `docs/design-docs/agent-harness.md`" — the base correctly ships the generic *template* `agent-harness.md` (it carries the `{{COMPONENTS}}` index R6.2 drift-checks); the strip-item is the instance doc. (3) **proposed (review-reliability):** widen RELIABILITY **R22**'s text from naming `lint_docs.py` to "every commit-gate lint step in `check.py` (`lint_docs`, `lint_structure`, `lint_base`, …)" so future gate lints inherit the totality contract by rule. (4) **proposed (review-security, optional):** a one-line SECURITY T9 note that the checked-in `base/` is **content-only** (a host adopts via `harness-init`, never by raw-copying a base file into an exec slot). (5) **taste note (review-code-quality):** `lint_base.expected_files` has a dual contract (returns the dict AND appends to a caller `errors` list) — a deliberate builder pattern (mirrors `lint_docs` check_* functions), documented, acceptable as-is. | Minor | 2026-06-21 | [packaging/06 gate](completed/2026-06-21-packaged-base-artifact-legacy-strip.md) | fixed (garden 2026-06-21: items 1–4 promoted — DESIGN `## Scripts` got the script→script-import prohibition + the `base/` rendered-not-baked discipline; the parent spec R6.4 was amended to name the self-host *instance* `agent-harness.md` (base ships the template); RELIABILITY R22 was widened from `lint_docs` to every `check.py` gate lint step; SECURITY T9 got the `base/`-is-content-only note. Item 5 taste note accepted as-is) |
| **Packaging Slice 5 — marketplace skill list is hand-maintained, not gate-checked (+ 2 proposed rules).** `.claude-plugin/marketplace.json`'s plugin description names the skill set in freeform prose, which `check.py` does NOT validate (unlike `docs/generated/component-inventory.md`, which `gen_inventory.py --check` enforces). That ungated prose drifted — it listed the deleted `dream` and omitted the present `docs-nav` until Slice 5 hand-fixed it. Two sub-items: (a) the list follows the spec's logical/workflow order, not the inventory's alphabetical order, so the two can't be eyeball-reconciled (kept the spec's R5.1 order; the inventory is the source of truth). (b) **Proposed rule (review-arch):** a low-cost *substring-containment* lint (not a prose parser — that brittleness killed the plan's Approach B): assert every `plugin/skills/*/` dirname appears in the marketplace description and no deleted-skill token lingers. Build only if the drift recurs. **Proposed rule (review-spec-compliance):** a spec convention that when a downstream slice's consistency requirement (R5.2) rests on an upstream documented deviation (Slice 1 kept `tidy_stop` vs R1.6's "delete it"), the downstream plan cites the deviation inline so the surviving artifact isn't read as drift. | Minor | 2026-06-21 | [packaging/05 gate](completed/2026-06-21-plugin-cleanup-manifest-update.md) | open (garden 2026-06-21: no promotion this pass, by discipline — the marketplace substring-containment lint is recurrence-gated ("build only if the drift recurs"); the upstream-deviation-cite spec convention judged too niche for a permanent PLANS.md rule. Both left tracked) |
| **Packaging Slice 4 (completion gate) — 2 P2s + 1 proposed rule:** (1) review-reliability: the driver-layer bare-call posture default at `director/run.py:225,260` + `director/orchestrator.py:522,818` is an **intentional** conservative `"untrusted"` (documented `run.py:232-234`; pinned by `test_default_posture_is_untrusted`) — a deliberate second tier (real runs resolve config → pass `on-request` explicitly; bare calls fall back to the most-conservative posture). It is NOT stale drift, but its un-named literal form got re-flagged by reviewers as a drift surface; promote it to a named constant (e.g. `run.CONSERVATIVE_POSTURE`) and/or one-line doc, OR make the driver posture a required kwarg so no fallback literal exists. (2) review-code-quality: stale `qa`-skill prose pointers in `docs/product-specs/2026-06-16-worker-qa-and-serialized-pr-merge.md:109,128` (Korean Design/components) → deleted `director/workspace_skills/qa/SKILL.md`; the same spec also carries pre-Slice-3 `docs/DIRECTOR.md` mentions (122,131) left untouched — historical-record staleness in a non-archived instance spec (D5-clean prose; not shipped in the base per Slice 6 R6.4). Gardening: add a one-line supersession note or accept as historical (Slice-3 precedent). (3) review-arch proposed rule: codify **signature-default aliasing via a module constant** under ARCHITECTURE invariant 5 — its "alias from DEFAULTS" examples are all module constants; this slice extends it to a function-signature parameter default (`approval_policy: str = _DEFAULT_APPROVAL_POLICY`), the correct pattern (a None-sentinel would re-introduce divergence; the `inspect.signature` drift test only works because the value is bound at the signature). | Minor | 2026-06-21 | [packaging/04 gate](completed/2026-06-21-two-agent-profiles-consolidated.md) | partially fixed (garden 2026-06-21: item 2 — a one-line supersession note added atop the 2026-06-16 qa spec (qa skill retired, DIRECTOR.md relocated), historical Korean prose left unrewritten per the Slice-3 precedent; item 3 — ARCHITECTURE `director/` invariant 5 extended to signature-default aliasing via a module constant. Item 1 — the driver-layer conservative `untrusted` → a named constant — is a CODE refactor, NOT doc-debt; left tracked) |
| **Committed test-runtime leak + a now-stale `qa` fixture (gitignore-anchoring gap).** `tests/.claude/harness/` carries **8 tracked files** that are leaked test-run output — a populated worker workspace `director-workspaces/ABC-9/.codex/skills/` (written by `test_director_linear.py`'s `--linear ABC-9 --mock` run) + `director-history/runs.jsonl`. The root `.gitignore` `.claude/harness/` has a *middle* separator so git anchors it to the repo root (`<root>/.claude/harness/` only) — it does NOT cover `tests/.claude/harness/`, so the runtime state escaped the ignore and got committed (`f5211f7`). Not load-bearing (no test reads the fixture; workspaces regenerate fresh), but packaging Slice 4's qa retirement now leaves a **stale `qa/SKILL.md` in the committed fixture** (the live `director/workspace_skills/` no longer ships it). Fix (its own change, touches `.gitignore` = live exec surface): `git rm -r --cached tests/.claude/harness/` + widen the ignore to cover it at depth (`**/.claude/harness/` or an unanchored leaf), verifying `test_scaffold`'s gitignore assertions (which test scaffold.py's *generated* gitignore, independent of the repo's) stay green. | Minor | 2026-06-21 | packaging/04 gate (out-of-scope find — pre-existing leak, surfaced by the qa retirement) | fixed (2432176, garden 2026-06-21: `git rm -r --cached` the 8 leaked `tests/.claude/harness/` files incl. the stale `qa` fixture; the root ignore was widened `​.claude/harness/` → `**/.claude/harness/` so harness runtime state is ignored at any depth — verified both root and `tests/` paths now match. `scaffold.py` GITIGNORE_LINES left as-is: a ported host doesn't run `director/` tests, so the leak only ever arose in this self-host repo; `test_scaffold` stays green 14/14) |
| **Packaging Slice 3 (completion gate) — 2 unwritten rules + a plan-wording nit:** (1) review-arch + review-reliability: no written rule governs the `docs/` ↔ `.claude/` boundary for *prose/config* docs — ARCHITECTURE invariant 5 only fences gitignored runtime state (`.claude/harness/`) out of `docs/`; this slice established de-facto that a tracked **central-agent operating manual belongs in `.claude/`, not the `docs/` knowledge graph**. Promote to an ARCHITECTURE invariant / DESIGN.md line so future moves (and the AGENTS map-row-membership question) have an authority to cite. (2) review-spec-compliance + review-reliability: a **"retire = grep the surviving doc/file body for the retired name, not just its inbound links"** hygiene discipline — D5 catches broken markdown links but not a stale prose self-reference; the missed §0 launcher-invoke (round-1 P1) would have been caught mechanically by this. Candidate DESIGN.md skills/docs-hygiene line. (3) Minor plan-wording: the M3 DoD verification grep should exclude `product-specs/` too (it states that scope in prose but the convenience command lags) — a per-plan author convention, not a machine rule. | Minor | 2026-06-21 | [packaging/03 gate](completed/2026-06-21-director-relocation-launcher-retirement.md) | fixed (garden 2026-06-21: item 1 — ARCHITECTURE invariant 5 extended (tracked central-agent config like `.claude/DIRECTOR.md` belongs in `.claude/`, not the `docs/` knowledge graph); item 2 — DESIGN `## Skills` got the "retire = grep the surviving bodies, not just the links" hygiene rule. Item 3 plan-wording nit = per-plan author convention, no machine doc, n/a) |
| **Packaging Slice 2 (completion gate) — 3 unwritten rules surfaced by reviewers:** (1) review-code-quality + review-arch: DESIGN.md's "map not encyclopedia / every sentence load-bearing" bar is unwritten for `harness-init` **seed templates re-read at runtime** (PRINCIPLES.md, PRODUCT_SENSE.md, the index guides) — same token-cost footing as SKILL.md; add a one-line DESIGN.md note so template prose-density is enforceable, not just a fix-forward nit. (2) review-arch: a DESIGN.md line that seed templates frame purpose via the **centralized consumption model only by reference** (never assume the host runs the Director locally) — the boundary the PRINCIPLES seed now respects. (3) review-reliability: name `scaffold.py`'s invariants in RELIABILITY (idempotent never-overwrite `seed()`; SEEDS/TOP_INDEXES stay **disjoint**; every INDEXED_DIR gets exactly ONE `index.md`; the lone `inventory` subprocess fails-soft and should carry a `timeout=`). Promote each on a second occurrence (feedback-twice). | Minor | 2026-06-21 | [packaging/02 gate](completed/2026-06-21-strict-base-docs-guidance-enrichment.md) | partially fixed (garden 2026-06-21: items 1+2 — folded into one DESIGN `## Skills` bullet: harness-init seed templates re-read at runtime sit on the same map-not-encyclopedia bar AND frame purpose via the centralized consumption model by reference only. Item 3 — naming `scaffold.py`'s invariants in RELIABILITY — left tracked, feedback-twice (promote on a second occurrence)) |
| **F3 (use-all shakedown):** `director.run`/`drive` used a **30s** `read_timeout_s` default AND `drive()` did not catch `ReadTimeout` — so the first real Codex worker that went silent >30s (cold start, a long `check.py`/test run, deep reasoning) crashed the whole ticket with an uncaught traceback (no `failed` disposition, no retry). The 684 prior mock runs never exposed it (the fake app-server answers instantly). | Important | 2026-06-20 | use-all shakedown canary (LIN-23 real run) | fixed (config default `read_timeout_s` 30→180s; `drive()` catches `ReadTimeout`→recoverable `{kind:failed, status:read_timeout}`; tests in test_director_run/config) |
| **F1 (use-all shakedown):** the orchestrator dispatched **every** untyped `Todo` ticket as a raw-prompt worker — so Linear's default onboarding issues (LIN-1 "Get familiar with Linear", etc.) sitting in the ready state were run as real (~700k-token) workers. The ready lane is assumed clean; nothing skipped label-less tickets. | Important | 2026-06-20 | use-all shakedown (board contamination) | fixed (opt-in `director.dispatch_requires_label`; `eligible_tickets(require_label=)` drops `taxonomy.ticket_type is None`; threaded through run_once/until_drained/forever; default False = pre-3b compat; tests in test_director_orchestrator/config) |
| **F2 (use-all shakedown):** worker economics — one *trivial* docs ticket reported **~5.77M tokens** in a single turn. Rollout analysis (the Codex session log) decomposes it: ONE turn = **64 model calls** (an agentic loop — each call re-sends the full+growing transcript, 30k→130k tokens), so input = Σ(growing ctx) ≈ 64×91k. But **97.5% was prompt-CACHE hits** (5.59M of 5.73M cached); effective NEW compute was only **~174k tokens** (140k fresh-in + 34k out). So $-cost is ~10× less than the headline; the real ceiling is the **rate window** (69% of the 5-hr Codex window on one ticket) IF cached tokens count toward it (confirm). Controllable drivers: broad orient (34 git + 22 reads + 15 list/find + 6 search), **7 full check.py runs whose output is re-carried every step**, ~30k skills/system baseline. Levers (impact order): (1) a "lite" worker path for low-risk tickets (skip full-repo orient, targeted docs) selected by a size/risk label; (2) run the gate ONCE near the end, don't retain full 685-test output in context; (3) context hygiene (don't re-read files already in the transcript). | Important | 2026-06-20 | use-all shakedown canary (LIN-23 rollout analysis) | partially mitigated (prompt-level levers shipped: `taxonomy.WORKER_PROTOCOL` adds a "proportional context — orient only as the ticket needs, nav is a tool not a forced step, don't re-carry large logs" discipline; `_IMPL_TEMPLATE` adds gate-once-near-completion cadence; AGENTS.md + agents-md template Orient softened to proportional. Structural lever (1) — a size/risk-labelled lite worker template — still open; the prompt nudges don't HARD-cap orientation.) |
| **F4 (use-all shakedown):** orchestrator daemon shutdown orphaned worker subprocesses — a single SIGTERM logged `stopped_reason: shutdown` (graceful-drain intent) but the spawned `codex app-server` children kept running and burning tokens; they had to be killed manually by workspace cwd. Reaping covers the orchestrator process, not its worker process tree. (May be partly a background-launch/process-group artifact — needs a clean repro under `--daemon` + SIGTERM before a code fix; R-rule candidate: a draining daemon must terminate its in-flight worker process tree, not just stop claiming.) | Important | 2026-06-20 | use-all shakedown (graceful-drain teardown) | open |
| **F8 (use-all shakedown, T2b):** the commit gate depends on a **gitignored** directory. `docs/symphony-original/` is gitignored (0 tracked files), yet 9 `docs/product-specs/*.md` hard-link to `../symphony-original/SPEC.md` / `WORKFLOW.md`, which `lint_docs` D5 checks against the **filesystem**. So the gate passes only on a machine where that untracked dir happens to exist locally — **any fresh clone** (a Director worker workspace, a ported host, or a new contributor cloning canonical) fails `lint_docs D5` on 6 broken links. Surfaced when shakedown worker LIN-25 correctly escalated rather than scope-creep-fixing it. Fix options: track `symphony-original/` (commit it), or drop/relativize the product-spec hard-links, or add the links to `.harnessignore` / mark them external. The first is simplest; decide intent (is it vendored reference meant to stay local?). | Important | 2026-06-20 | use-all shakedown T2b (LIN-25 baseline-gate escalation) | fixed (lint_docs D5: a broken link whose target resolves under a `.harnessignore`'d tree is treated as EXTERNAL, not broken — `_under_exempt`; total over unresolvable paths. Verified on the real corpus: gate GREEN with symphony-original absent. Chosen approach: teach-D5-to-skip, NOT track-the-dir, preserving the deliberate gitignore.) |
| **F7 (use-all shakedown):** the harness does not act on Codex **rate-limit exhaustion**. When the account is out of credits / past its window (`rate_limits.credits.has_credits=false`, or an exhausted primary window — captured in worker telemetry), the worker returns **empty turns** (`task_complete` with 0 agent messages / 0 tool calls). In WATCHED mode this surfaces as repeated empty `turnReview`s (`final_message=None`, no `outcome`) the Director can only answer "continue" → a no-progress loop; un-watched it would burn turns to `stuck`. The harness captures `rate_limits` (R12 telemetry) but never short-circuits on exhaustion. Improvement: detect `has_credits=false` / exhausted window in `drive`/the decider and **park** the ticket with a distinct `rate-limited — awaiting reset` reason (a known terminal, like `needs_human` → escalate), instead of empty-turn looping. | Minor | 2026-06-20 | use-all shakedown (LIN-25 first attempt, rate-limited) | fixed (`run._rate_limited` detects exhaustion — `credits.has_credits=False` or a spent `primary` window, case-tolerant + total — and `drive` parks via an `escalate` disposition before `decide`, short-circuiting the empty-turn loop; in the shared dispatch path so orchestrator/merger workers inherit it. Tested.) |
| Cross-run history producer/consumer dir asymmetry: `director/dashboard.py` grew a `--history-dir` flag + `history_dir` on `_DashboardServer`, but `director/orchestrator.py` `main()` does NOT thread `history_base` and there is no `paths.history_dir` in `config.DEFAULTS["paths"]`, so the orchestrator's history WRITE and the dashboard's READ converge only via the shared default `.claude/harness/director-history` or `$DIRECTOR_HISTORY_DIR` (the common same-repo case works; an explicit dashboard `--history-dir` with no orchestrator twin silently shows an empty panel). Full symmetric fix = add `history_dir` to the `.harness.json` `paths` config + thread it through `orchestrator.main()`/`run_until_drained`/`run_forever` (the run fns already accept `history_base`), matching how `status_dir`/`queue_dir` flow. **Proposed rule** (review-arch): a new operator-redirectable state dir under `.claude/harness/` must be reachable by the same precedence (explicit `base=` → env → `paths.<name>` → default) on EVERY process that reads or writes it — a knob on one process but not its peer is a drift bug. | Minor | 2026-06-19 | cross-run-history completion gate (review-arch P2 + proposed rule) | open |
| Cross-run history spec under-specifies two fields (review-spec-compliance proposed rules): (1) the `ended_at` source is unpinned — the impl defaults it to the final snapshot's `updated_at` (reasonable, since `status.finished()` runs immediately before the hook); (2) the `GET /api/v1/history` route's N is unpinned — the impl uses `read_history`'s default `RECENT_RUNS_MAX=50`. Pin both in a future spec rev if they ever matter. Also: `summarize` persists `passes` (from `run.pass`) but the panel's `fmtRun` never renders it — a forward-looking field, harmless. | Minor | 2026-06-19 | cross-run-history completion gate (review-spec-compliance proposed rules) | open |
| Two SSE/streaming rules the dashboard slice followed by code but unwritten: (1) **streaming heartbeat-liveness** — a long-lived read stream must force a periodic write (heartbeat) so a silently-dead peer is detected within a bounded interval AND bound its loop on server liveness (an injectable `should_run`/shutdown flag), not only on a failed write, so an in-process `shutdown()` ends quiet connections promptly (generalize RELIABILITY R14, which today only covers disconnect-mid-write); `director/dashboard.py` `_stream_loop` honors both (`_STREAM_HEARTBEAT_S`, `serving` Event). (2) **SSE framing convention** — event-stream responses send NO Content-Length, `\n\n`-delimited `data:` frames, `Cache-Control: no-cache`, HTTP/1.0 close-delimited body; unwritten in DESIGN/ARCHITECTURE. Promote both so the next streaming surface inherits them. | Minor | 2026-06-19 | dashboard-sse-rate-limit completion gate (review-arch + review-reliability proposed rules) | open |
| Inline-page-JS testability ceiling: `director/dashboard.py`'s client logic (`fmtRateLimits`, `startStream`/fallback, `render`) lives in the single `PAGE` HTML string and cannot be Python-unit-tested without a JS runtime — so e.g. `fmtRateLimits` rendering (`{"remaining":9}`/`null`/`{used_percent:42}`) is covered only by PAGE string-presence asserts + the per-slice playwright E2E, not executable CI. Pre-existing architectural property of the single-HTML-string page (the server-side `build_view`/`_stream_loop` were pulled OUT of the page precisely so they could be unit-tested). Options if it ever bites: a tiny JS test harness, or accept the ceiling and lean on the behavioral E2E. | Minor | 2026-06-19 | dashboard-sse-rate-limit completion gate (review-code-quality P2) | open |
| Layer-2 live token display shows retry-burn transiently: queued usage from a FAILED attempt can apply to the retried in-flight entry (orchestrator `drain_accrual` runs after `reap`-which-retries), so live `in_flight[].tokens` / the run total briefly include a failed attempt's usage until the new attempt accrues over it. LIVE-DISPLAY ONLY — the final aggregate is correct (`terminal()` folds only the final attempt). Deferred per the observability-polish spec's explicit "retry-burn token accounting" non-goal; a proper fix resets `tokens=None` in `retrying()` AND drops queued usage for a retried tid (or drains before reap). | Minor | 2026-06-18 | layer2-token-accrual completion gate (review-code-quality P2) | deferred (spec non-goal) |
| Two `director/` architecture rules the Layer-2 marshal followed by imitation but unwritten: (1) ARCHITECTURE invariant 7 should name "a new per-tick MAIN-THREAD side-channel drain (e.g. `drain_accrual`) rides the shared batch/daemon loop, placed at the SAME seam — after `reap` — in both `_dispatch_wave` and `run_forever`, never re-derived" so the planned SSE change-detect / Phase-B history flush inherit it; (2) the pure telemetry extractors `extract_usage`/`extract_rate_limits` arguably belong in a `director` CORE module both `orchestrator` and `worker` depend on, not inside the `worker/app_server` transport (invariant 8 spirit) — D-2 chose reuse-in-place for minimal blast radius. Promote (1) if a second per-tick drain lands (feedback-twice→promote). | Minor | 2026-06-18 | layer2-token-accrual completion gate (review-arch proposed rules) | open |
| Reliability rule unwritten: a callback bound into an un-isolated producer loop (e.g. an `on_event` handed to `AppServerClient.run_turn`, which invokes it WITHOUT try/except) must be exception-total at its OWN boundary (catch-all → drop/record) — a raise there propagates into and aborts the observed turn, so instrumentation would silently gate the primary path. Generalizes R12 ("the extractor is total") to "the whole observation callback is total". `orchestrator._enqueue_usage` now honors this by code (`try/except: pass`); promote to RELIABILITY.md (compose with R12/R14). | Minor | 2026-06-18 | layer2-token-accrual completion gate (review-reliability proposed rule) | open |
| DRY: `director/status.py` `accrue` (L156-158) and `terminal` (L180-183) open-code the byte-identical all-or-nothing `{input,output,total}` int-group validation (`isinstance(v,int) and not isinstance(v,bool)`); the two consume it differently (replace vs. accumulate). A `_token_group(d) -> dict|None` helper would centralize the "matching terminal's fold" contract the `accrue` docstring promises in prose, so the two can't drift. Deferred at the gate: touches `terminal()` (reviewed as unchanged this slice); fix-forward, not a blocker. | Minor | 2026-06-18 | layer2-token-accrual completion gate (review-code-quality P2) | open |
| Boundary error handling: ast.parse (lint_structure.check_imports) and json.loads (gen_inventory.build) raise raw tracebacks on malformed input instead of FAIL+FIX | Minor | 2026-06-12 | Phase 0-1 quality review M1 | open |
| Security threat model T1–T9 deferred/scoped (user call): T1/T2/T4/T5/T6/T7 guard the DISABLED memory loop (dormant); review-security descoped from the mandatory completion gate to the live exec surface only (hooks · .harness.json/.claude/lints · .harnessignore). Reactivate the dormant threats WITH the memory-loop redesign — the threat model co-evolves with the loop it guards | Minor | 2026-06-13 | user scoping decision | deferred (reversible) |
| Adding a plugin component (skill/agent) retroactively reddens every already-ported host's gate: D9 (check_coverage) requires the host's hand-docs mention every plugin component AND gen_inventory --check needs a host inventory regen. Surfaced when shipping architecture-setter broke Lingual's gate. Decide: accept as a documented "plugin-update → host resync" step, or weaken D9 on hosts to only components the host references | Minor | 2026-06-13 | host-taste-setter M5 (Lingual) | fixed (flexible-host-governance: D9 and generated inventory now run by default only when the plugin is self-hosted; ported hosts can opt in with `.harness.json` `component_coverage: strict` / `component_inventory: strict`) |
| D8 registration uses substring match (`plan.md` counts as registered via `master-plan.md`) | Minor | 2026-06-12 | Phase 0-1 quality review M4 | open |
| tests run_all() duplicates main() check lists — new checks can silently escape green-path tests; extract shared CHECKS tuple | Minor | 2026-06-12 | Phase 0-1 quality review M5 | open |
| Fresh-clone fragility: empty untracked dirs (plugin/skills etc.) make lint_structure raise FileNotFoundError until later phases populate them | Minor | 2026-06-12 | Phase 0-1 spec review | open |
| doc-gardener drift scan bumps last_verified on fresh pages (erodes D4 signal); gate step 2 on pages near the 30-day threshold | Minor | 2026-06-12 | Phase 2 quality review #3 | open |
| garden/dream `git add -A` safety silently depends on .claude/harness/ being gitignored — document the coupling | Minor | 2026-06-12 | Phase 2 quality review #4 | fixed (portability gate: scoped adds) |
| imprint_run stale-lock break is TOCTOU (two workers can both break it; lock.stat may race FileNotFoundError) | Minor | 2026-06-12 | Phase 3-5 spec review P2-1 | open |
| imprint_run: uncaught TimeoutExpired/OSError per entry — poison entry stalls every drain ~900s and is never marked; add per-entry catch + attempt cap | Important | 2026-06-12 | Phase 3-5 spec review P2-2 | open |
| state files grow unbounded (imprint-queue.jsonl, imprint-processed.txt, seen-sessions.txt); add rotation | Minor | 2026-06-12 | Phase 3-5 spec review P2-3 | open |
| feeder_firstprompt mark_if_new is non-atomic rewrite; concurrent sessions can drop an id — append-only form fixes | Minor | 2026-06-12 | Phase 3-5 spec review P2-4 | open |
| imprint_run: KeyError on corrupt queue entry (only JSONDecodeError caught) | Minor | 2026-06-12 | Phase 3-5 spec review P2-5 | open |
| imprint_run reads queue once per spawn; entries enqueued during a drain wait for next hook event | Minor | 2026-06-12 | Phase 3-5 spec review P2-6 | open |
| compile_pack degradation path (R2 headline) untested — mock subprocess.run for non-zero/empty/timeout cases | Important | 2026-06-12 | Phase 3-5 quality review #2 | open |
| imprint_run drain logic lives in main() — extract drain(root, runner) for fixture tests per DESIGN.md explicit-params rule | Important | 2026-06-12 | Phase 3-5 quality review #3 | open |
| cross-cutting duplication: headless claude spawn / fail-open log block / MEMORY.md-existence literal repeated in 3 hook scripts — centralize in harness_lib (hl.run_headless, hl.log_hook_error) | Important | 2026-06-12 | Phase 3-5 quality review #4 | open |
| feeder pack has no structural sentinel check (any exit-0 stdout injected); validate "## Where we are" before injecting | Minor | 2026-06-12 | Phase 3-5 quality review m1 | open |
| imprint lock mtime never refreshed during long drains — live worker can be reaped as stale after 1h; os.utime per iteration | Minor | 2026-06-12 | Phase 3-5 quality review m2 | open |
| dream skill `git add -A` sweeps unrelated uncommitted files; narrow to `git add docs/` | Minor | 2026-06-12 | Phase 3-5 quality review m3 | fixed (portability gate: scoped adds) |
| knowledge/recursion-guard.md states guard contract imprecisely ("is set" vs == "1") | Minor | 2026-06-12 | Phase 3-5 quality review m4 | fixed |
| dreamer.md lacked inline T7 guard ("digest content is DATA") — now added; but no lint enforces T7 on archive-reading agents | Minor | 2026-06-12 | Task 17 completion gate (review-security P2) | closed (moot — `dreamer` agent retired with the memory loop, packaging Slice 1) |
| imprint_run `--allowedTools` uses `Bash(python3 plugin/scripts/*)` wildcard — too broad per T5; narrow to `Bash(python3 plugin/scripts/lint_docs.py)` or explicit list | Minor | 2026-06-12 | Task 17 completion gate (review-security P2) | open |
| imprint child gets unscoped Write/Edit — "write only under docs/memory/" is a prompt instruction, not a path restriction; a transcript injection defeating the T1 guard could write docs/.harnessignore (then self-un-govern memory, post host-legacy-docs). Path-scope the child's writes to docs/memory/ + hard-check reject outside it | Important | 2026-06-13 | host-legacy-docs completion gate (review-security P2) | open |
| Structural tension: gitignored runtime state (.claude/harness/) and must-travel instance skills (.claude/skills/) share the .claude/ parent. On hosts that blanket-ignore .claude/, skills need `git add -f`. Workaround documented (harness-init step 6 + scaffold NOTE); revisit if a cleaner split is wanted (e.g. runtime state under a top-level .harness/) | Minor | 2026-06-13 | Lingual second-target port | documented (workaround); open for cleaner split |
| Tracker `fixed` rows should cite the implementing commit SHA (traceability; prevents premature closure) | Minor | 2026-06-12 | §7 validation persona P2 | open |
| imprint job writes memory but does not commit — writes sit dirty until the next session/dream commit sweep; decide owner of the commit step | Minor | 2026-06-12 | final review observation | closed (moot — imprint job retired with the memory loop, packaging Slice 1) |
| agent-harness.md embeds a components snapshot ({{COMPONENTS}}) that duplicates generated/component-inventory.md and can drift — but a plain pointer breaks D9 for hosts (check_coverage excludes generated/ from its hay); resolve jointly | Minor | 2026-06-12 | portability gate (review-arch P2) | open |
| scaffold writes follow symlinked destinations (seed/git_hook write_text); refuse `is_symlink()` targets | Minor | 2026-06-12 | portability gate (review-security proposed) | open |
| lint readers crash on non-UTF8 .md in host docs (bare read_text in lint_docs/lint_structure); tolerate with errors=replace or skip+FAIL | Minor | 2026-06-12 | portability gate (review-reliability proposed) | open |
| no instance-layer S3 analog: nothing stops a machine-local absolute path being committed into a versioned host doc | Minor | 2026-06-12 | portability gate (review-arch proposed) | open |
| T7 extension candidate: any hook output injected into agent context that quotes repo-file content must carry the inline DATA guard (tidy_stop now does; rule not yet in SECURITY.md) | Minor | 2026-06-12 | portability gate (review-security proposed) | open |
| pre-commit hook body is a bare exec — missing python3 / dead gate path yields a raw shell error with no FIX hint; worktrees (`.git` file) and core.hooksPath silently get no hook | Minor | 2026-06-12 | portability gate (review-reliability proposed) | open |
| Lint thresholds (STALE_DAYS 30 / former D1 120 / former D7 400 / former MEMORY 60) and the MACHINE_DOCS tuple are hardcoded in plugin source — ported hosts lack the self-host "change the rule in the same commit" escape; design one host-config mechanism jointly with HARNESS_LINT_CMD (G3) at second-target porting, driven by which constants the target actually fights | Minor | 2026-06-13 | post-gate portability audit | fixed then superseded — line caps were removed; `.harness.json` still overrides D4 (`stale_days`), wires `HARNESS_LINT_CMD`/`lint_cmd` host-lint, and lets `architecture-setup` author per-repo app-code lints. MACHINE_DOCS stays plugin-side by design (the machine's own grounding contract, not a host-tunable threshold) |
| director/queue append_request dedupe is read-before-append (no lock): concurrent duplicate delivery of the same request_id can double-append. Phase 1 is single-worker/single-Director so it can't yet bite; harden (O_APPEND+lock or atomic claim) when Phase 2 adds concurrency | Minor | 2026-06-14 | director Phase 1 completion gate (codex review P2-1) | fixed (orchestrator-dispatch-loop M2: append_request now serializes dedupe-read+append under module `_APPEND_LOCK`; regression test proves 40 concurrent same-id appends dedupe to 1, distinct appends never corrupt. Single-process scope — cross-process O_APPEND/flock still open for Phase 4 multi-process) |
| director/worker/app_server: while the seam blocks in wait_for_answer, the client stops reading the subprocess, so an app-server that dies mid-approval is detected only at answer timeout/BrokenPipe, not promptly; add a liveness check / monitor | Minor | 2026-06-14 | director Phase 1 completion gate (codex review P2-2) | open |
| director worker `linear_graphql` exposes UNRESTRICTED Linear GraphQL (destructive mutations / deletes) to the worker under the .env key — Symphony-faithful by design, but before autonomous (un-watched) dispatch it needs an explicit worker authority boundary: read/write scope, a mutation allowlist, or Director approval for mutations | Minor | 2026-06-14 | worker-tooling completion gate (codex review P2) | fixed (worker-authority-guardrail, Phase 4 first slice, 0db4f3a: `director/worker/authority.py` classifies the GraphQL op and default-denies any non-allowlisted mutation root field before any POST; reads always pass; guard on by default in `make_linear_tool_executor`. SECURITY.md T10. Residual deferred by design: argument-level policy + escalate-denied-mutations-to-Director → the taste-vs-handle escalation slice) |
| director/board/linear.py `list_ready_issues` fetches the full `inverseRelations` node set with no `type` filter and filters `type=="blocks"` in Python — on boards with large relation graphs this over-fetches. No client-side fix unless Linear's `inverseRelations` connection grows a `filter:` arg; revisit if payload size bites | Minor | 2026-06-14 | dag-aware-orchestration completion gate (review P2-E) | open (accepted tradeoff) |
| director/orchestrator.py run_until_drained poll-failure uses a broad `except Exception` (intentional: any poll error → clean `poll_failed` stop, not a crash) — could swallow a programming error (e.g. KeyError) in list_ready_issues. Acceptable for thin watched loop; narrow to HTTP/transport errors if a real bug ever hides here | Minor | 2026-06-14 | dag-aware-orchestration completion gate (review note) | open (accepted tradeoff) |
| director/status.py `StatusWriter._flush` (and the SAME pattern in director/queue `write_answer`) leaves an orphan `*.tmp` if the process is SIGKILLed between `mkstemp` and the `finally` cleanup — reads stay correct (os.replace atomic), but stale temp files accumulate under high-crash-frequency. Single-writer so safe to glob-unlink stale `.tmp` on StatusWriter init; do it jointly with the queue when either bites | Minor | 2026-06-15 | director-orchestration-visibility completion gate (review-reliability P2-A) | open (accepted tradeoff) |
| Worker + `network_access=true` exfiltrates host secrets, bypassing T10 — now applies to **BOTH watched and un-watched** (network is shared; modes differ only by turn-end decider). Live-probed (codex 0.139.0): `workspace-write` reads are filesystem-wide (worker read the repo by absolute path from a workspace outside it), so `.env`/`LINEAR_API_KEY` is readable regardless of workspace location; full outbound network then lets a prompt-injected worker POST the key anywhere. **Workspace relocation does NOT mitigate (reads are fs-wide).** Human decision (2026-06-15): network ON for both modes, exfil to be fixed **once, holistically at the credential layer** — either egress via a `network_proxy` domain allowlist (`-c features.network_proxy.enabled=true -c 'features.network_proxy.domains={...}'`) OR removing host secrets from disk behind a secret manager / env-injection proxy (so a fs-wide read finds nothing). Until then BOTH postures are safe only with throwaway credentials. | **Major** | 2026-06-15 | worker-autonomy-config gate (review-security P1.1, live-confirmed) + posture unification | partially mitigated — **env-inheritance channel CLOSED** (worker-secret-boundary M1, 2026-06-16): `director/worker/policy.py` builds a deny-by-default worker env (operational base + host-allowed `.harness.json worker_policy.worker_env`) passed to `Popen(env=...)`, so a worker no longer inherits host secrets via the environment. **Still open (fs-wide read + egress): DEFERRED to the always-on/cloud deployment (Decision 2026-06-16; SECURITY.md T11), NOT built locally.** Three findings: (1) codex cannot read-scope — all 3 sandbox modes read fs-wide (`readable_roots`/`sandbox_permissions` rejected), so the on-disk read only closes under isolation; (2) the worker already brokers (`linear_graphql` runs in the Director, not the sandbox — the only leak is the fs-wide read); (3) a local secret-manager only raises the bar (bootstrap-credential recursion: the gcloud ADC that auths to it is itself on-disk-readable). So container + egress-enforcement + secret-manager/WIF merge and land at cloud-deploy time (container = deploy unit, WIF = off-disk identity). Local self-host: INFORMED-accepted, safe only with throwaway creds. |
| Worker can read codex's OWN model auth (`~/.codex`) and any other on-disk credential — the deny-by-default worker env (M1) closes the *env* channel but the worker is `codex app-server`, a brain+hands process that needs its model auth to run, so its auth file is fs-reachable. Closing this is the container plan (secretless image) + the GCP Secret-Manager vault-proxy plan (capability mediation): a secretless worker requests actions, the proxy holds creds; codex's model key becomes an ephemeral/brokered token, not a long-lived host file | Minor | 2026-06-16 | worker-secret-boundary M1 (design observation) | open (deferred to container + vault-proxy plans) |
| director/worker/app_server.py `AppServerClient(env=None)` still means "inherit the parent env" — production spawns are secure (the ONLY construction is `run._prepare`, which always builds the deny-by-default env), but a future *direct* constructor could silently bypass the boundary. Make `env` required (or an explicit `inherit_parent_env=True` test-only escape hatch) when the API is next touched | Minor | 2026-06-16 | worker-secret-boundary M1 (codex review P2) | open (theoretical — no production direct caller today) |
| director/orchestrator.py `reconcile`: a FAILED Done-write still reports `final_state: "done"` in the summary (the failure IS surfaced via `reconcile_error`, but the labeled final_state is optimistic). Pre-existing best-effort pattern across all branches (set_state then label); making final_state reflect the write outcome needs set_state to return success and a broader touch. Low-harm (error visible). | Minor | 2026-06-15 | multi-turn-ticket-execution completion gate (codex review P2 + review-arch P2-B-adjacent) | open (accepted — error surfaced via reconcile_error) |
| director watched decider: `wait_for_answer` blocks an orchestrator POOL thread (holding the worker subprocess open) up to `timeout_s` per turn end → worst-case pool occupation `timeout_s × max_turns × concurrency` before any frees. Intended (a no-answer must surface, not auto-continue) but wanted a dedicated per-turn-review timeout knob (separate from the approval seam's `timeout_s`). | Minor | 2026-06-15 | multi-turn-ticket-execution completion gate (review-reliability P2-B/P2-D) | fixed (orchestrator `--turn-review-timeout` knob threads into `make_queue_decider`; `--read-timeout` knob added too, both during the real-LIN tier-2 validation. Pool-occupation nature is inherent to watched mode; the knob bounds it) |
| Worker→`mergeRequest` enqueue handoff not wired: the QA+serialized-merge slice built+live-verified the merge MECHANISM (queue + `director/merger.py` drain + Director `mergeReview` surface), but nothing yet enqueues a `mergeRequest` when a worker finishes done+PR — so R4's done→queue feed is not end-to-end. Wiring needs an optional PR field on `report_outcome` (worker/tools.py) + the drive sink capturing it + `orchestrator.reconcile` calling `queue.append_merge_request` on terminal-done-with-PR. Deferred by human decision (ship mechanism, wire handoff as a follow-up) — touches orchestrator + report_outcome schema (shared with the secret-boundary session) | Important | 2026-06-16 | worker-qa-and-serialized-pr-merge completion gate (spec review P1, human-deferred) | fixed (activate-serialized-merge-pipeline M1, 9a8bd67: report_outcome gained pr_url/pr_branch; reconcile._maybe_enqueue_merge enqueues on terminal-done-with-PR. Live-verified end-to-end with real codex BOTH ends: local-merge wire-pin + full gh PR roundtrip — a real worker opened a real GitHub PR and the merger squash-merged it; see the plan Outcomes) |
| Merger re-enqueue-after-Director-guidance loop deferred: `merge|<ticket>` / `mergereview|<ticket>` dedupe one-open-per-ticket by design, so an `answer_merge_review(action=requeue)` cannot auto re-queue under the same id. Needs a fresh attempt/PR discriminant on the merge ids + the loop that calls it (who requeues, when). Spec Open Q; today live resolutions are human/abandon (DIRECTOR.md §7) | Minor | 2026-06-16 | worker-qa-and-serialized-pr-merge completion gate (codex review P1/P2) | fixed (merge-reenqueue-loop, 2026-06-16: attempt discriminant on merge ids `merge|<t>|a<n>`/`mergereview|<t>|a<n>` + `director_min.requeue_merge` re-enqueues attempt+1 with the Director's guidance, max_attempts cap; end-to-end loop test converges. Also retired the re-escalation-dedup bug) |
| `director/worker/autonomy.py:36` comment claims `workspace-write` forces `.git`/`.codex` read-only by Codex, but a live probe (codex-cli 0.139.0) showed an in-sandbox worker's `git commit` SUCCEEDS — `.git` IS writable under `workspace-write`. Doc-accuracy nit (no functional impact; the M4 wire-pin relied on the real behavior, and it matters for the secret-boundary threat model — a false ".git read-only" overstates what the sandbox protects) | Minor | 2026-06-16 | worker-qa-and-serialized-pr-merge M4 (live probe) | fixed (autonomy.py:36 comment corrected to state `.git` IS writable under workspace-write, live-probed) |
| director/watch.py runReport uses the single status.json (latest-state) snapshot: if a 2nd orchestrator run overwrites it within one watch poll interval (~0.5s) of the 1st finishing, the 1st run's terminal runReport is missed. Not hit by the watched single-run model (one run_until_drained at a time, human/LLM-paced >> poll). Fix = a durable terminal-event append-log, but that edges toward the rejected durable-record/separate-channel direction; revisit if rapid back-to-back runs become real | Minor | 2026-06-16 | director-board-reporting completion gate (review-reliability P1->P2 downgraded) | open (accepted tradeoff — snapshot-tail design) |
| director/worker/app_server.py `extract_usage` token-event shape shipped pinned to the SPEC §13.5 documented names (flat `thread/tokenUsage/updated` / `total_token_usage`) but UNCONFIRMED — the M4 live-pin hit `usageLimitExceeded`. | Minor | 2026-06-16 | worker-telemetry-capture M4 (live-pin blocked by usage limit) | fixed (2026-06-16, credits restored: live-pinned against real codex-cli 0.139.0 — the actual shape NESTS absolute totals under `params.tokenUsage.total` (`{totalTokens,inputTokens,outputTokens,cachedInputTokens,reasoningOutputTokens}`) alongside a `last` per-turn delta, NOT the flat SPEC shape, so the original extractor returned None on real data. Reconciled via `_absolute_from_wrapper` (descends to `.total`, never `.last`); mock `usage` scenario now emits the real nested shape; `test_real_codex_0139_nested_shape` pins the exact payload. Live-confirmed end-to-end: `run_turn` captured `{input:26399,output:40,total:26439}` on a real turn. rate_limits also confirmed via `account/rateLimits/updated`.) |
| director run aggregate (`status.py` codex_totals) is FINAL-ATTEMPT cost: tokens burned on a failed-then-retried attempt aren't folded (each `drive` starts with fresh usage=None; reconcile's retry path returns `{retry:True}` without `summarize`, so only the terminal attempt's telemetry reaches the StatusWriter). Not a double-count and cannot crash — a cost-telemetry UNDER-count. Fix = accumulate per-ticket across attempts in `_dispatch_wave` (or keep final-attempt semantics and leave documented). Documented inline (status.py terminal comment); chosen as accepted semantics for this slice. | Minor | 2026-06-16 | worker-telemetry-capture completion gate (review-reliability P2.1) | open (accepted — final-attempt semantics, documented) |
| Two host-architecture rules the telemetry slice followed by imitation but that aren't written down: (1) host telemetry/instrumentation extractors must be TOTAL functions — None/0 on malformed input, never raise, never block the primary path (lives only implicitly in status.py R3 + the `agent_message_text` idiom); (2) the StatusWriter is a main-thread LOCK-FREE single writer — cross-thread mutation forbidden (lives only in the status.py module docstring; would steer the Layer-2 live-in-flight-accrual follow-up that reaches for on_event). Promote both to the host architecture doc so future observability slices inherit them by contract. | Minor | 2026-06-16 | worker-telemetry-capture completion gate (review-arch + review-reliability proposed rules) | fixed (2026-06-16: promoted to **RELIABILITY.md R12** [telemetry/instrumentation extractors are total — None/0 on malformed, never raise, never gate] + **R13** [status/snapshot writers are main-thread lock-free single-writers; no cross-thread mutation; Layer-2 accrual must marshal to the main thread]. Now numbered, citable rules under review-reliability's grounding doc — `director/status.py` already cited RELIABILITY.md R9, so this is the established host-runtime contract home; ARCHITECTURE.md/DESIGN.md are `plugin/`-machine-scoped) |
| `director/*` runtime-module architecture rules followed by imitation but unwritten (no citable rule binds `director/`, which lives outside `plugin/`'s S-series layer law): (1) `director/*` runtime modules are stdlib-only; (2) they take explicit `base=` overrides and never read cwd/env directly outside their `_root`; (3) any NEW network listener must be `127.0.0.1`-bound, fixed-route, and read-only by default; (4) the pure-core / HTTP-shim / inline-page testability split (logic unit-tested without a socket). The dashboard honors all four by taste. Promote to the host architecture doc (compose with the row above — both are "write down `director/` host-architecture invariants so reviewers have authority"). | Minor | 2026-06-16 | director-observability-dashboard completion gate (review-arch proposed rules) | fixed (promoted to ARCHITECTURE.md "Host runtime (`director/`) invariants" §, rules 1–4: stdlib-only · explicit `base=` · loopback/fixed-route/read-only listeners · pure-core-thin-transport; review-arch now grounds in it) |
| HTTP / read-API boundary fail-soft rule unwritten: RELIABILITY.md frames fail-open only for hook scripts (R6) and queue workers (R8) — the dashboard is the repo's first HTTP request-thread boundary, covered by no numbered rule. The rule the slice followed: a read-only observability/instrument listener must treat the FULL client-disconnect errno family (`OSError`/`ConnectionError`, not just `BrokenPipeError`/`ConnectionResetError`) as a quiet drop, and must never let an in-handler exception reach `socketserver.handle_error` (which prints a traceback to stderr) — degrade to a structured error response instead. `director/dashboard.py` now honors this by code; generalize R6 to long-lived stdlib listeners so the next one inherits it. | Minor | 2026-06-16 | director-observability-dashboard completion gate (review-reliability proposed rule) | fixed (promoted to RELIABILITY.md R14 — read-API listeners fail soft: handler bug → structured error response, full client-disconnect errno family → quiet drop, nothing reaches socketserver.handle_error/stderr; R6 generalized to request threads) |
| `director/` "every default that also lives in `config.DEFAULTS` must alias it (no parallel literal in ANY director module, merger included)" is enforced only by per-slice enumeration — the declarative-config plan listed `autonomy/orchestrator/run` and silently omitted `merger.py`'s `DEFAULT_MAX_MERGES` (caught + fixed in-gate). Write the alias discipline into ARCHITECTURE.md invariant 5 (or DESIGN.md) so it is enforceable, not re-derived per slice. | Minor | 2026-06-16 | director-declarative-config completion gate (review-arch proposed rule) | fixed (2026-06-18: ARCHITECTURE.md `director/` invariant 5 — every default that also lives in `config.DEFAULTS` is aliased from it, no parallel literal in ANY director module incl. merger) |
| Config/host-policy loaders split **fail-open(absent) vs fail-loud(present-but-malformed)**, and the fail-loud raise MUST precede the first irreversible side effect (board claim/transition, worker spawn, lock). Now a 3-instance pattern (`harness_lib.gate_config`, `policy.load_worker_policy`, `director.config`) with no numbered RELIABILITY rule — R6/R8/R14 cover hooks/queue-workers/read-API listeners, not startup config gating. Promote to a numbered RELIABILITY.md rule so future config surfaces cite it (feedback-twice→promote; compose with the prior director/ rule promotions). | Minor | 2026-06-16 | director-declarative-config completion gate (review-reliability proposed rule) | fixed (2026-06-18: RELIABILITY.md **R15** — config/host-policy loaders fail-open(absent)/fail-loud(present-malformed), raise before the first irreversible side effect) |
| Cross-thread coordination rules unwritten (RELIABILITY companion to R13): active-run-reconciliation is the first main-thread→worker-pool-thread mutable signal (`cancel_event`). The slice does it right — the ONLY cross-thread object is a thread-safe `threading.Event` (main sets, worker reads `is_set`); `futures`/`cancel_events`/`cancelled_states`/`attempts` are mutated only on the main thread. Promote two rules: (1) any new cross-thread coordination object MUST be a thread-safe primitive (Event/Queue), never a plain dict/list reached into from a pool thread; (2) cooperative cancellation of a long-running subprocess worker MUST bound observation latency (here `_CANCEL_POLL_S`) AND guarantee no stale signal cancels a fresh attempt (here fresh-Event-per-submit + pop-on-completion). | Minor | 2026-06-16 | active-run-reconciliation completion gate (review-reliability proposed rules) | fixed (2026-06-18: RELIABILITY.md **R16** — cross-thread coordination via thread-safe primitives only; cooperative cancel bounds latency + is stale-safe) |
| DESIGN notes unwritten (review-arch proposed): (1) a terminal summary's `final_state` is the orchestrator's best OBSERVATION of the board state (the state the ticket ends in), not an internal disposition label — the `released`-vs-observed-state P2 only arose because no rule pinned this (fixed in-gate: cancelled now reports the observed external state); (2) the reconcile cadence is WALL-CLOCK-anchored (`last_reconcile` monotonic), not wake-anchored, so it fires under steady completions (R2) — defended only by a code comment today; a one-line note guards stage-2 (continuous tick) from regressing it. | Minor | 2026-06-16 | active-run-reconciliation completion gate (review-arch proposed rules) | fixed (2026-06-18: ARCHITECTURE.md `director/` invariant 7 — folds both reconcile semantics: `final_state` is an observation; reconcile cadence is wall-clock-anchored) |
| director/orchestrator.py `run_forever`: the `claim_failed` set grows for the daemon's lifetime (suppresses per-tick re-claim spam, D-65) AND treats a TRANSIENT claim failure (board hiccup) as PERMANENT — a ticket whose claim write once raised/returned False is excluded until restart, no recovery. Acceptable per D-73 (rare on a curated board); proper fix = TTL / backoff re-admission, which is the gap #3 backoff work (it already owns the `_idle_wait_s` seam). Pairs with the unbounded-growth note (bounded by distinct failing ids). | Minor | 2026-06-17 | continuous-daemon-loop completion gate (review-reliability P2) | open (accepted per D-73; deferred to gap #3 exponential-backoff slice) |
| Daemon-architecture rules followed by the slice but unwritten: (1) cooperative-cancel must cover RE-SUBMITTED (retry) work — a force-stop that cancels only current workers can be defeated by a reap-spawned retry (fixed in-gate via `_RunState.born_cancelled`); generalize mark-before-act to the retry edge. (2) a daemon-lifetime EXCLUSION set (`claim_failed`) needs a liveness story (TTL/backoff re-admission or documented restart-only recovery) so a transient failure isn't permanent. (3) `_RunState` is the NAMED sharing boundary between the batch wave and the daemon — future loop modes (gap #3) route claim/reap/reconcile through it, never re-derive. (4) a test-only unbounded-loop bound (`max_ticks`) may break with in-flight futures unreaped — document that prod wiring never sets it. Promote the load-bearing ones to ARCHITECTURE.md/RELIABILITY.md so the next loop-mode slice inherits them by contract. | Minor | 2026-06-17 | continuous-daemon-loop completion gate (review-arch + review-reliability proposed rules) | fixed (2026-06-18: ARCHITECTURE.md `director/` invariant 7 [_RunState the one batch/daemon sharing boundary; no-new-worker-during-drain is structural] + RELIABILITY.md R16/R17 [cancel-covers-retry staleness; daemon-map eviction on set-exit]; the `max_ticks` test-only caveat stays a code comment) |
| Daemon-lifetime maps keyed by ticket id must be evicted when the ticket LEAVES the relevant set, not only on the success path — the general form of the gap-#3 claim-backoff leak (claim_fails/claim_retry_at popped only on successful claim → a transient-claim-failed ticket that a human then moves/deletes leaked for the run; fixed in-gate by GC-ing tids not in `ready ∪ in_flight` each poll). Now a 3-instance pattern (`results` via retain_results=False, `attempts` terminal-pop, claim-backoff GC) → promote to a numbered RELIABILITY.md rule so future daemon state inherits the eviction discipline by contract. | Minor | 2026-06-17 | daemon-exponential-backoff completion gate (review-arch + review-reliability, same P2 — fixed in-gate; rule unwritten) | fixed (2026-06-18: RELIABILITY.md **R17** — daemon-lifetime state keyed by ticket id is evicted on set-exit [terminal / moved-off-board], not only on the success path; GC against `ready ∪ in_flight`) |
| Daemon-architecture rules now PROVEN load-bearing by the closed daemon track, still unwritten (parked since stage 2): (1) `_RunState` is THE named batch/daemon sharing boundary — gap #3 routed retry through it via `reap(on_retry=…)` and rejected re-deriving; (2) a force-stop's "no new worker spawns during a drain" is a STRUCTURAL guarantee (scheduled-retry + `not draining`-guarded due-retry + drain-exit-on-empty-futures), which SUPERSEDED and removed stage-2's `born_cancelled` patch; (3) active-run reconciliation reach = `state.futures` only, so a ticket in a non-future holding map (`pending_retry`) is human-cancel-blind until its retry submits (accepted short window; board-as-truth converges). The daemon track (gaps #1/#2/#3) is now CLOSED — the natural moment to promote (1)/(2) to ARCHITECTURE.md `director/` invariants and (3) to a RELIABILITY accepted-staleness note. | Minor | 2026-06-17 | daemon-exponential-backoff completion gate (review-arch + review-reliability proposed rules) | fixed (2026-06-18: ARCHITECTURE.md `director/` invariant 7 [(1) _RunState boundary, (2) drain structural guarantee that superseded `born_cancelled`] + RELIABILITY.md R16 [(3) futures-only reconciliation reach = accepted bounded staleness]) |
| Two doc-rules the worker-protocol slice followed but that aren't written down (review-arch proposed, non-blocking): (1) `run.drive` is THE single first-turn framing seam — the worker's first-turn protocol blocks (`WORKER_PROTOCOL` + `TERMINAL_CONTRACT` via `frame_first_turn`) are injected there and NOWHERE else; later turns carry the decider directive verbatim. R1's "all dispatch paths" guarantee rests on this being the one injection point, but it lives only in a code comment + the plan Context. Promote to an ARCHITECTURE.md `director/` invariant so a future double-injection (a caller framing again before `drive`) is reviewable/lint-able. (2) No DESIGN.md taste rule for "worker-prompt protocol text is the product — keep disciplines terse, stage-agnostic in the shared preamble vs stage-specific in the template, map-not-encyclopedia"; the slice honored it via the spec's Non-goals only. Capture so the next prompt-enrichment slice (e.g. slice 2's decider-side text) has an authority to cite. | Minor | 2026-06-17 | worker-operating-protocol completion gate (review-arch proposed rules) | fixed (2026-06-18: ARCHITECTURE.md `director/` invariant 6 [`run.drive` is THE single first-turn framing seam] + DESIGN.md Skills [agent/worker operating-protocol prose is product text — terse, preamble-vs-template, map-not-encyclopedia]) |
| director/orchestrator.py `--autonomous` help text (ap.add_argument, ~L927) still reads "un-watched: use the code turn-end decider" — the old binary framing, not the three-mode no-agent niche (spec R1 / DIRECTOR.md §6). The authoritative reframe (DIRECTOR.md §6) and merger.py's matching help text landed this slice; only this secondary string remains. Deferred because orchestrator.py was being concurrently modified by another session and hunk-level staging (`git add -p`) is unavailable in this environment, so the one-line edit could not be isolated without entangling the other session's uncommitted work. Fix = update the help string to mark `--autonomous` as the no-agent / `--mock`-CI / truly-detached path once the file is uncontended. | Minor | 2026-06-17 | lights-out-director completion gate (review-arch P2) | fixed (2026-06-18: file uncontended after the concurrent session committed; `director/orchestrator.py` `--autonomous` help text now reads the no-agent / `--mock`-CI / truly-detached niche and points at DIRECTOR.md §6's three-mode model, mirroring merger.py) |
| DESIGN.md "one persona ↔ one grounding doc (1:1, lint S5)" is now imprecise: the new `review-code-quality` grounds in TWO docs (DESIGN.md + core-beliefs.md) and `review-spec-compliance` grounds in per-plan docs (the ExecPlan / linked product-spec) rather than one fixed doc. The S5 lint only checks that a `review-*` body cites some `docs/` path, so both pass — but the prose 1:1 rule no longer literally describes the persona set. Fix = amend the rule to "1:1 with a *primary* grounding doc (additional constraint docs / per-plan specs allowed)". | Minor | 2026-06-17 | execplan-qa-review-gate completion gate (review-arch proposed rule) | fixed (2026-06-18: DESIGN.md amended — "one persona ↔ one **primary** grounding doc; additional constraint docs / per-plan inputs allowed alongside") |
| harness-init seed templates (`plugin/skills/harness-init/templates/plans-md.md`, `agents-md.md`) still carry the OLD review-budget framing (review_level-only), now that the self-host added an always-on spec-compliance + code-quality QA pair. Arguably correct-by-design (hosts adopt/amend their own review policy per DESIGN.md "host vs machine") — but there is no written rule disambiguating *machine-universal* gate behavior (should propagate to seeds) from *host-policy* (stays customizable). Decide + write a one-line DESIGN.md rule, then propagate if universal. | Minor | 2026-06-17 | execplan-qa-review-gate completion gate (review-code-quality proposed rule) | fixed (2026-06-18: DESIGN.md "Host vs machine" rule — machine-universal gate behavior [the always-on QA pair] propagates to seeds, host policy stays customizable; AND propagated: harness-init `plans-md.md`/`agents-md.md` seed templates now describe the always-on spec-compliance + code-quality pair) |
| SECURITY.md has no threat-model entry for the Director **operator-console write surface** (`POST /api/v1/answer`) or the **outbound notifier egress** (`director/notify.py`) — the first first-party Director write surface + outbound sender (T10/T11 cover the worker sandbox, not these). The code honors the invariants the security review enumerated — (write) `127.0.0.1`-only, per-server `secrets.token_urlsafe` CSRF token + `compare_digest` + loopback Origin/Host fence, request_id constrained to an already-queued id (no `answers/<id>.json` traversal), body validated before write; (egress) operator-configured URL kept in `.env`, http(s)-only, queue-metadata-only payload (no secrets) — but none is written as a numbered threat. Promote to SECURITY.md T12 (console write surface) + T13 (notifier egress) so future diffs on these are graded against them. | Minor | 2026-06-18 | director-operator-console completion gate (review-security proposed T12/T13) | fixed (2026-06-18: SECURITY.md **T12** [operator-console write surface — loopback + CSRF token + Origin/Host fence + validated first-party input] + **T13** [notifier egress — secret URL in .env, http(s)-only, queue-metadata-only payload]; added to the live-surface status note) |
| RELIABILITY.md has no "**write-surface result fidelity**" rule: an operator write surface that fans a request to a downstream writer which can REFUSE (idempotent no-op, cap-exceeded, already-queued) must propagate that refusal — never a generic success envelope for a no-op. The instance is fixed (the `mergeReview` requeue path returns non-200 when `requeue_merge` refuses), but R12/R14 cover read-instrument totality + listener fail-open, not "a write the surface reported written that the writer declined." Promote to a numbered RELIABILITY rule so future `director/` write routes inherit it. | Minor | 2026-06-18 | director-operator-console completion gate (review-reliability proposed rule) | fixed (2026-06-18: RELIABILITY.md **R18** — write-surface result fidelity: propagate a downstream writer's refusal, never a generic success envelope for a no-op) |
| `director/notify.py` imports the module-private `dashboard._summary_for` (+ `HUMAN_BOUND_KINDS`) cross-module — a sibling transport reaching into another's privates. Acceptable now (console+notifier are one slice; duplicating the summarizer would drift the ping from the console view; self-flagged "promote if a third consumer appears") and review-code-quality concurred. No written `director/` rule says where a shared pure helper / cross-module contract should live (a non-transport core module) vs. private-import from a sibling. Promote a rule if/when a third consumer arrives. | Minor | 2026-06-18 | director-operator-console completion gate (review-arch proposed rule; review-code-quality concurred) | fixed (2026-06-18: ARCHITECTURE.md `director/` invariant 8 — shared director/ helpers live in a core module; a cross-module private import is tolerated for a two-consumer slice, promote on the third) |
| No written `director/` rule for daemon **diagnostic stream discipline**: structured operational diagnostics (`poll_failed`/`poll_recovered`, the new `_startup_recovery` `startup_cleanup_skipped`/`orphan_*`) go to **stderr** while **stdout** is the program's data-payload stream (`main()` result JSON). The code now conforms (this slice fixed the `_startup_recovery` prints to `file=sys.stderr`), but the convention is only implicit. Promote a one-line rule (RELIABILITY or a host-runtime invariant) so a future daemon-diagnostic print is graded against it, not re-discovered. | Minor | 2026-06-18 | symphony-adapter-parity completion gate (review-arch proposed rule) | open (doc-debt — code conforms; convention unwritten) |
| ARCHITECTURE invariant 8 ("shared director/ helpers live in a core module") should name `run.workspace_path`/`run.is_contained` as the canonical single-derivation example (consumed by dispatch + merge-enqueue + startup cleanup + reconcile cleanup), alongside the existing examples, so a future re-derivation of `root/str(tid)` is caught by reference. Not a code gap — the helpers already exist and are shared. | Minor | 2026-06-18 | symphony-adapter-parity completion gate (review-arch proposed rule) | open (doc-debt — example naming) |
| The workspace `hook_timeout_s` default `60.0` appears as a literal in three places: `config.DEFAULTS["workspace"]` (source of truth), the `reconcile`/`drive`/`_prepare`/`run_ticket` signature defaults, and `_RunState.reap`'s `_dispatch_kwargs.get("hook_timeout_s", 60.0)` fallback. Consistent with the repo's per-function-default convention (mirrors config defaults) and the `_dispatch_kwargs`-bag pattern, but the magic number is now duplicated. Accepted as-is (centralizing across config.py/run.py/orchestrator.py adds a cross-module import for marginal gain); revisit if the default changes. | Minor | 2026-06-19 | workspace-lifecycle-hooks completion gate (review-code-quality P2) | open (accepted — mirrors the per-function-default convention) |
| `director/merger.py` `merge_outcome` scans merge attempts `1..20` (`_MERGE_ATTEMPT_SCAN`) while the requeue cap is a hardcoded `max_attempts=3` (`director_min.py`). Safe + fail-closed today (20 ≫ 3; `max_attempts` has no config path that could exceed the scan bound; an out-of-range answered land reads `unresolved` = ticket stays merging = the SAFE direction, never a false `landed` onto a stale main). But the `20` is an unanchored literal whose correctness depends on an unstated per-ticket merge-attempt ceiling. Flagged by review-arch + review-reliability + review-code-quality (3×) at the merge-gated-eligibility gate. Fix = derive the scan bound from the same config cap it shadows (or assert `>=` it), and/or a written RELIABILITY rule: a bounded scan over a retryable queue dimension must be ≥ its config cap AND fail closed, so the two constants can't silently drift into a dropped land. | Minor | 2026-06-19 | merge-gated-eligibility completion gate (review-arch + review-reliability + review-code-quality proposed rule, 3×) | open (fail-closed + documented; anchoring deferred — no host-configurable max_attempts today) |
| `director/merger.py` defers a CI-pending PR correctly WITHIN a drain pass (per-pass `deferred` set → no head-of-line block), but across `run_loop` polls the PR stays in `pending_merges`, so each poll re-`drain`s → `process_request` re-captures the intended diff (gh) AND re-drives the FULL LLM land lane before the gate defers again. Bounded by the poll sleep (no CPU busy-spin) and never corrupts/loses the PR — but it re-runs an expensive worker turn every poll while CI runs, contradicting R3's "no busy-spin" intent for the pending case (the spec assumed "retried on a later pass" would be a cheap re-check). Fix = cross-poll "prepared" state (remember the request is prepared + its ORIGINAL pre-rebase intended diff) so a deferred PR's retry runs the code gate ONLY, skipping the land-lane re-drive; or back off the poll cadence for deferred-only queues. Needs care: the retry must reuse the ORIGINAL intended (re-capturing post-rebase would equal actual and blind the tripwire). | Minor | 2026-06-19 | merge-preservation-hardening completion gate (review-arch P2) | fixed (deferred-merge-gate-only-retry, be92601: `run_loop` owns a `{request_id: original pre-rebase intended}` cross-poll cache, threaded `run_loop`→`drain`→`process_request`. A cached (already-prepared, CI-pending) PR's later poll runs the code gate ALONE against its stored original intended — no land-lane re-drive. Entry planted on prepared+deferred, popped on terminal, orphans GC'd at drain entry; `prepared=None` (default / `--once`) == pre-fix behavior. Chose an in-memory cache over a queue-payload mutation (the queue is append-only — an in-place rewrite would race worker appends) or a persisted marker (stale-marker risk for a rare-restart gain); a restart cold-starts the cache == pre-fix behavior, no regression. Also closed a LATENT CORRECTNESS gap: the pre-fix per-poll re-capture read the already-rebased branch (== actual) and blinded the tripwire across a defer; reusing the stored original keeps the comparison meaningful. 5 tests, 614 green) |
| `director/merger.py` `drain` reads `pending_merges(base=base)` twice at the top of a pass: once for the prepared-cache orphan-GC (computing the live request_id set) and again as the loop's first `pend` read. Harmless — both reads are under the single-consumer flock (no race), it is one redundant small-file parse PER PASS (not per-PR), and the loop's read must stay fresh (it re-reads each iteration as it consumes). Flagged by review-arch + review-reliability + review-code-quality (3×), all rating it non-blocking and advising AGAINST de-duping (coupling the GC to the loop's first iteration for marginal gain). Fix-forward only if a future change already touches the drain-entry reads. | Minor | 2026-06-19 | deferred-merge-gate-only-retry completion gate (3× reviewers, non-blocking) | open (accepted; de-dup explicitly not worth the coupling) |
| No written rule for **in-memory consumer-side caches that gate an irreversible act** (the merger's `prepared` cross-poll cache; the orchestrator's `claim_failed` set). review-arch + review-reliability each independently proposed one at the deferred-merge-gate-only-retry gate. Proposed (ARCHITECTURE "Host runtime invariants" + RELIABILITY): a process-lifetime cache that gates an irreversible side effect MAY live in process memory rather than `.claude/harness/` PROVIDED (a) it is keyed on the durable item's identity so a new attempt cannot collide with a stale entry, (b) it is evicted on a terminal result AND GC'd against the live pending set, (c) the sole writer is enforced (flock), and (d) its loss degrades to the pre-cache (re-derive-once) path, never to a stale-value act. This is the consumer-side dual of R17 (daemon-lifetime state evicted on set-exit) but for a cache feeding an irreversible merge, which R17 does not cover. Not applied this slice (scope = the merger fix); land in a doc-gardening pass. | Minor | 2026-06-19 | deferred-merge-gate-only-retry completion gate (review-arch + review-reliability proposed rule) | open (doc-debt — proposed, not yet written) |
| `gen_inventory.py` (`fm.get("description","")[:100]`) and `scaffold.py` (`[:90]`) slice the `description` value assuming it is a `str`; now that `read_frontmatter` can return a list, a page authoring `description: [a, b]` would render `['a','b']` into the inventory/component table instead of text (no crash, deterministic — no gate flip; no current page does this). Coerce display keys with `d if isinstance(d, str) else " ".join(d)`. | Minor | 2026-06-18 | [knowledge-format-phase1](completed/2026-06-18-knowledge-format-phase1.md) completion gate (review-reliability P2) | open |
| Proposed RELIABILITY.md rule: a parser shared between a gate and instrumentation (e.g. `harness_lib.read_frontmatter`) must keep ALL gate consumers total — when the parser widens a value's type (here `str` → `list`), every downstream gate consumer that assumed `str` (`fromisoformat`, slicing, `.startswith`, regex) must degrade to a clean FAIL/skip, never an uncaught exception that aborts the lint run. Generalizes the D4 `(ValueError, TypeError)` fix; R12 scopes totality to observation-only extractors and explicitly excludes gates, so no current rule covers a gate-parser totality regression. | Minor | 2026-06-18 | [knowledge-format-phase1](completed/2026-06-18-knowledge-format-phase1.md) completion gate (review-reliability proposed rule) | open (doc-debt) |
| Proposed RELIABILITY.md rule: a corpus-walking read-only tool (`nav.py`) must isolate per-page and per-edge — any parse/resolve error on a single page or link is skipped, the tool still emits a complete result for the rest and exits 0. The `_resolve_links` out-of-root `relative_to` crash (fixed in-gate with try/except) was a fail-soft violation this rule would have caught by contract; today nav covers it ad hoc (`_as_list` for tags, `is_stale` skip for bad dates, the link guard). One occurrence — promote on recurrence. | Minor | 2026-06-18 | [knowledge-navigation-tool](completed/2026-06-18-knowledge-navigation-tool.md) completion gate (review-reliability proposed rule) | promoted → RELIABILITY.md R21 (2026-06-19, third recurrence) |
| Proposed DESIGN.md rule (Scripts): graph projections split by multiplicity — a **flat edge view** (`nav.relations`) mirrors the link list 1:1 (a page linking a target twice → two edges), while a **tree/adjacency view** (`nav._adjacency`/`tree`) collapses to one edge per `(src,dst)` node pair (a hierarchy must not show duplicate children). The slice honors + tests both halves, but no DESIGN rule states when a projection preserves vs collapses graph multiplicity. | Minor | 2026-06-19 | [nav-derived-hierarchy](completed/2026-06-19-nav-derived-hierarchy.md) completion gate (review-arch proposed rule) | open (doc-debt) |
| Proposed DESIGN.md rule (Scripts): inference/relation tables (`nav.EDGE_RULES`) are harness taste → live in **code, not `.harness.json` config**, same grain as lint/DESIGN rules (D-56 declarative-config precedent). Sound + matches "methodology stays in code" (ARCHITECTURE invariant 7), but the precedent lives in a memory ADR, not the grounding docs. | Minor | 2026-06-19 | [nav-derived-hierarchy](completed/2026-06-19-nav-derived-hierarchy.md) completion gate (review-arch proposed rule) | open (doc-debt) |
| Proposed RELIABILITY.md rule: read-only navigator/projection emitters (the `nav.py` `_emit_*` family) must be **total over empty result sets** — never index `[0]` or assume non-empty (the `_emit_tree` empty-non-forest `IndexError` was unreachable-but-latent, fixed in-gate). Extends R12 to the corpus-query surface; composes with the row above on per-page/per-edge isolation. | Minor | 2026-06-19 | [nav-derived-hierarchy](completed/2026-06-19-nav-derived-hierarchy.md) completion gate (review-reliability proposed rule) | promoted → RELIABILITY.md R21 (2026-06-19) |
| `nav.py tree` reverse/unfiltered render was noisy — it followed the generic untyped `links`/`linked-by` edges (incidental markdown mentions) mixed with typed ones, so a hierarchy needed manual `--rel` to be readable. | Minor | 2026-06-19 | [nav-derived-hierarchy](completed/2026-06-19-nav-derived-hierarchy.md) dogfooding (live repo scan) | fixed (2026-06-19: `tree` now follows only typed relationships by default — drops the untyped `links` fallback — with `--all` to include them; reverse tree of the Phase-1 spec went 26→11 lines. Tests + docs-nav skill updated) |
| `nav.py orphans` signal was diluted: all 34 exec-plans (a terminal/historical tier with no `exec-plans/index.md`, orphaned by design) dominated the 46-orphan list, burying genuinely-unreachable knowledge pages. | Minor | 2026-06-19 | [nav-derived-hierarchy](completed/2026-06-19-nav-derived-hierarchy.md) dogfooding (live repo scan) | fixed (2026-06-19: `orphans --type <T>` restricts to one tier — `orphans --type knowledge/adr/openq/limitation` all return 0, confirming the knowledge corpus is fully connected and the noise was entirely the historical-plan tier. Tests + skill updated) |
| Proposed RELIABILITY.md rule: generalize R12 (telemetry/instrumentation extractors are total) to explicitly cover the read-only **nav.py projection surface** (`roadmap`/`tree`/`drift`/`relations`/`_emit_*`) — a projection over the doc corpus must be a total function: malformed/absent `phase`, a non-numeric `NN`, a supersession cycle, a multi-spec plan, and an empty corpus must all degrade to a rendered/empty result, never raise. Code already satisfies this (verified at the gate); the contract is unwritten for the nav surface. | Minor | 2026-06-19 | [charter-and-progress-map](completed/2026-06-19-charter-and-progress-map.md) completion gate (review-reliability proposed rule) | promoted → RELIABILITY.md R21 (2026-06-19) |
| Proposed KNOWLEDGE_FORMAT §2.2 authoring convention: `phase` `NN` uniqueness-within-an-initiative is unwritten. The roadmap orders deterministically by `(NN, label)` so collisions render safely (the dogfooded Symphony initiative has two `03-*` and six `04-*` by design), but there is no rule on whether `NN` must be unique or may be deliberately shared (concurrent slices). Write the convention if a uniqueness expectation is wanted. | Minor | 2026-06-19 | [charter-and-progress-map](completed/2026-06-19-charter-and-progress-map.md) completion gate (review-arch proposed rule) | open (doc-debt) |
| Decide + document whether the roadmap **initiative set is open or curated**: `roadmap()` admits any `phase` prefix as a first-class initiative (the live corpus surfaced a third, `methodology`, beyond the spec's named Symphony + knowledge-format). Consistent with "structure = projection" (initiatives are derived, not enumerated) and the right default; but if a curated/enumerated set is desired (e.g. cross-checked against the charter's Initiatives section) it must be written down. | Minor | 2026-06-19 | [charter-and-progress-map](completed/2026-06-19-charter-and-progress-map.md) completion gate (review-arch proposed rule) | open (doc-debt) |
| Proposed DESIGN.md rule (Scripts): the **volatility principle** for overview surfaces — inline what is *sparse and load-bearing* (pivots: `[superseded-by …]`), drill down on what is *high-volume and contextual* (follow-ups: a count badge on `map`, rows behind `followups`). This taste governs `nav.py map`/`roadmap` and prevented the pivot-flood, but lives only in the product-specs, not in DESIGN.md. Capture so future map-surface work is grounded. | Minor | 2026-06-19 | [map-depth-pivots-followups](../product-specs/2026-06-19-map-depth-pivots-followups.md) completion gate (review-arch proposed rule) | open (doc-debt) |
| Proposed DESIGN.md rule (Scripts): one shared path-resolver helper — D5, D11 (`resource`), and D12 (`supersedes`) each re-inline `(p.parent/t).exists() or (root/t).exists()`. Faithful to the cited D5 pattern, but a single helper (`_resolves(p, root, t)`) would prevent drift across the three copies. | Minor | 2026-06-20 | [format-governance-enforced-keys](../product-specs/2026-06-20-format-governance-enforced-keys.md) completion gate (review-arch proposed rule) | open (doc-debt) |
| D12 under-enforces a **list-valued** `resource`/`phase`: the `isinstance(res, str)` guard skips a misauthored list form silently (D11 keys instead FAIL on list form). Benign + fail-soft (consistent with R12 "prefer absent over wrong"; `resource` is 1/99), but asymmetric with D11. Optionally FAIL a non-str `resource`/`phase` for symmetry. | Minor | 2026-06-20 | [format-governance-enforced-keys](../product-specs/2026-06-20-format-governance-enforced-keys.md) completion gate (review-reliability observation) | open |
| **Open question CLOSED (migrated from retired `docs/memory/openq/`):** redesign the automatic memory loop. Resolution: the loop is **retired, not redesigned** — superseded by native Claude Code memory (packaging Slice 1). The feeder/imprint scripts, `dream` skill, `dreamer` agent, `MEMORY.md` bootloader, and `docs/memory/` were deleted; `tidy_stop` was kept (gate-on-stop net, sentinel re-pointed to `.harness.json`). The re-enable instructions are obsolete. | Minor | 2026-06-21 | [memory-subsystem-retirement](completed/2026-06-21-memory-subsystem-retirement.md) | closed (retired, not redesigned) |
| **Limitation resolved by removal (migrated from retired `docs/memory/limitations/`):** `progress/current.md` went stale when work bypassed hooked sessions. The file and the hooked feeder/imprint loop no longer exist; native CC memory + on-demand `docs/logs.md` replace the continuity role, so the failure mode is gone. | Minor | 2026-06-12 | [memory-subsystem-retirement](completed/2026-06-21-memory-subsystem-retirement.md) | closed (resolved by removal) |
| **Numbered-rule text still describes the retired memory loop:** `docs/RELIABILITY.md` (R2/R3/R7 — feeder fallback / imprint idempotency / mark-seen) and `docs/SECURITY.md` (T1/T2/T4–T7 — transcript→memory write-back, imprint least-privilege) carry numbered rules about feeder/imprint, and `docs/QUALITY_SCORE.md` has a "dreaming docs" grade row. SECURITY's status note already frames its T-rules as historical; the rest were left verbatim this slice (renumbering a rule set is non-trivial and reviewers did not block). Gardening pass: retire/renumber the memory-loop rule text across RELIABILITY/SECURITY/QUALITY_SCORE, or add a parallel "historical — loop retired" note to RELIABILITY. | Minor | 2026-06-21 | [memory-subsystem-retirement](completed/2026-06-21-memory-subsystem-retirement.md) completion gate (post-sweep residue) | fixed (garden 2026-06-21: chose the "parallel historical note" option, NOT a renumber — RELIABILITY got a status note framing R1–R5/R7 as the retired loop's lineage (R8–R22 + ~15 tracker rows + tests cite the numbering, so renumbering would shatter them); QUALITY_SCORE retired the `memory-store`/`feeder`/`imprint`/`dreaming` rows + grade notes; SECURITY already carried its status note. Also fixed adjacent drift: ARCHITECTURE's "Failure modes" section still described the retired feeder/imprint loop) |
| **Proposed: mechanize seed-template ↔ scaffold consistency.** This slice missed sweeping the `harness-init` seed templates (they described the deleted `docs/memory/`) because nothing asserts a seeded template references only paths the scaffold creates — the "edited self-host doc, forgot its template twin" class (core-belief 13). review-arch + review-reliability both proposed: a `test_scaffold` assertion / lint that no seeded template may reference a `docs/<root>/` not in `scaffold.DIRS` (mirrors `test_machine_docs_all_propagate_to_hosts`). | Minor | 2026-06-21 | [memory-subsystem-retirement](completed/2026-06-21-memory-subsystem-retirement.md) completion gate (review-arch + review-reliability proposed rule) | open (doc-debt) |
| **`tests/test_nav.py` fixtures still build their corpus under `docs/memory/`** (the retired path) — the tests PASS (nav indexes any frontmatter-bearing page regardless of managed-root status) but exercise stale path names rather than the new `docs/adr/`+`docs/design-docs/` shape (review round 2, both codex spec-compliance + code-quality, P2). M2 listed updating it; a mechanical path-prefix repoint broke inter-page relative links across the larger TestNavTree/Relations/Roadmap fixtures, so it was reverted to the passing original and deferred. Fix: repoint `memory/adr→adr`, `memory/knowledge→design-docs`, retire the `openq`/`session-digest`/`archive` fixture types, and fix the relative links + assertions in EVERY fixture (not just TestNavCatalog), running `test_nav` to green. | Minor | 2026-06-21 | [memory-subsystem-retirement](completed/2026-06-21-memory-subsystem-retirement.md) completion gate (review round 2, P2) | open (doc-debt) |
| **Worker first-turn framing token floor:** a trivial single-file ticket through a REAL worker spent **~107K input** tokens for ~1.4K output — dominated by `taxonomy.frame_first_turn` (WORKER_PROTOCOL + TURN_PROTOCOL preamble) + the agent's system prompt + tool/MCP defs, **not** the task. It is a per-turn FIXED floor re-sent every turn; it amortizes across a real multi-turn ticket but caps how cheap a worker turn can get. Sibling of **F2** (worker economics), isolated here as the framing floor specifically. Lever if cheap turns are ever wanted: a trimmed "lite" first-turn frame for low-risk tickets (composes with F2 lever (1), the size/risk-labelled lite worker path). | Minor | 2026-06-21 | cc-codex-appserver §9.3 live verification (Phase B real run, 108,585 tok) | open |
| **`--worker claude` runtime is approval-gated but NOT OS-sandboxed:** the Director sends `sandbox=workspace-write` (`director/worker/app_server.py:371`) but the `cc-harness-appserver` adapter (`dist/handlers.js:44-45` → `resolvePosture`, `dist/posture.js`) maps only `approvalPolicy`→`permissionMode` and **never consumes `params.sandbox`** — so a Claude worker keeps the auto_review/approval round-trip (T11 boundary 2) but loses codex's OS-level filesystem/network containment (T11 boundary 1). T11 defers FS/egress closure to the worker runtime; codex honors the `sandbox` posture, this adapter silently doesn't, so `--worker claude` is materially less contained than the default codex runtime. Default stays codex (opt-in gap; verified against the installed adapter, not the SDK docs). Fix: map app-server `sandbox`→an equivalent restriction in the producer (`SSFSKIM/codex_somersault` `cc-harness-appserver`) or have the adapter explicitly reject unsupported sandbox postures; until then treat `--worker claude` as approval-gated-only. **FIXED 2026-06-23 (producer PR #3 `0b7e205`; adapter source now vendored into this monorepo):** the adapter applies an OS-level sandbox (Seatbelt/bubblewrap Bash isolation + L3 cred-read deny) — **CONFIRMED live in the LIN-27 `--worker claude` dogfood**: the sandbox denied `test_director_dashboard`'s loopback socket bind, which the worker correctly read as an env artifact (not a regression). **SUPERSEDED BY DECISION 2026-06-23:** the OS sandbox is now intentionally **DISABLED** for `--worker claude` (human decision — rely on the Agent-SDK auto-mode permission classifier; the sandbox's friction, e.g. the loopback-bind stall above + global-cache writes + `jest` hangs, outweighed its marginal value before an isolated runtime exists). Mechanism: `director.worker_runtime_sandbox = {"claude": "danger-full-access"}` → adapter `resolveSandbox` short-circuits → no OS sandbox / deny rules / egress allowlist → classifier-only; codex untouched; PR3 code retained dormant. INFORMED-ACCEPTED residual (write-containment + cred-read deny lost; throwaway creds only) recorded in SECURITY.md; revisit at container/VM deploy (T11). See `completed/2026-06-23-claude-worker-classifier-only.md`. | Major | 2026-06-23 | PR #7 review → fixed `0b7e205`, dogfood-confirmed; OS sandbox then disabled by decision (2026-06-23) | superseded by decision — OS sandbox intentionally off for claude (classifier-only); see SECURITY.md + the classifier-only execplan |
| **Per-runtime security-posture override is an unwritten governed surface:** `director.worker_runtime_sandbox` (and any future per-runtime posture key) is the first config that can *downgrade* a runtime's security posture — unlike `director.worker.network`/`auto_review`, which only *tighten*. SECURITY.md should name it a Tier-0 executable-policy surface: git-visible, reviewed as a security change, and an override may only weaken the runtime it NAMES (never silently the default/codex runtime). The classifier-only diff already satisfies this in practice (codex provably untouched; override keys validated to name a configured runtime), but the invariant is unwritten — codify so a future runtime addition is held to it. | Minor | 2026-06-24 | claude-worker-classifier-only completion gate (review-security proposed rule) | open (proposed rule; behavior already satisfied — codify in SECURITY.md, doc-gardener) |
| **PR #7 worker-runtime toggle — secondary review findings:** (a) **`worker_env` is a flat GLOBAL allowlist** (`director/worker/policy.py:97`), so `CLAUDE_CODE_OAUTH_TOKEN` (added for the claude runtime) is also injected into the **default codex** worker's env (`director/run.py:300` builds the env without reference to the selected runtime) — a least-privilege deviation (T11). Proposed rule: when `director.worker_runtimes` declares >1 runtime, scope a runtime's credentials to that runtime (e.g. `worker_runtimes.<name>.env`). (b) **`network_allowlist` omitted the Anthropic host** — **FIXED 2026-06-23:** `api.anthropic.com` (the installed SDK's verified runtime egress host) added to `network_allowlist`; the broader "allowlist enumerates every declared runtime's API host" consistency-check remains a proposal. (c) **toggle-code robustness** (`director/config.py`) — **FIXED 2026-06-23:** `resolve_worker_command` now uses `cfg.worker_runtime if worker is None else worker` so an empty/blank `--worker` fails loud instead of silently using the default; `_build` rejects the reserved `codex` key in `worker_runtimes` (it tracks `codex_command`). Tested (`test_resolve_worker_command_empty_string_fails_loud`, `test_reserved_codex_runtime_key_rejected`). | Minor | 2026-06-23 | PR #7 review (review-security + codex; corroborated) | open — (a) `worker_env` over-exposure only; (b)+(c) fixed 2026-06-23 |
| **Watched-Director `terminal done` whose `outcome` omits `pr_url`/`pr_branch` BYPASSES the merge gate — a PR-bearing ticket reaches board-`Done` UNMERGED:** the watched terminal handler takes the PR from the **Director's free-form disposition** `disp["outcome"]` (`director/orchestrator.py:174`), and `_maybe_enqueue_merge` (`orchestrator.py:110`) reads `pr_url`/`pr_branch` from THAT — never from the worker's authoritative `report_outcome` (which carried them; cf. the `orchestrator.py:103` comment "the worker PROPOSES its PR"). `.claude/DIRECTOR.md` §4's disposition example also omits the PR fields, so a Director following the doc drops them → no PR detected → ticket goes **straight to `Done`** (no `merging` park, no `mergeRequest`) → the merger's **preservation tripwire + hygiene gate never run**, and board-`Done` stops meaning merged-to-`main` (a child `blocked_by` could build on an unmerged base). `--autonomous` passes the worker proposal through, so the gap is **watched-path only**. Surfaced by the LIN-27 `--worker claude` dogfood (2026-06-23): LIN-27 reached `Done` with PR #1 still open; recovered by manually enqueuing the merge (pr_url+branch) + `merger --once` (landed `5caebcd`). Fix: `_maybe_enqueue_merge` should fall back to the worker's captured `report_outcome` pr_url/pr_branch when the Director outcome lacks them (the worker proposal is authoritative); a PR-bearing `report_outcome` that resolves to a direct-`done` should log/escalate as a likely misfire, never silently land on `Done`; and fix the §4 example to carry the PR fields. **FIXED 2026-06-23 (`c081eac` + `f8fb6c9`):** `decider._backfill_pr_fields` backfills pr_url/pr_branch/evidence from the worker's `report_outcome` into a terminal-done disposition the Director omitted (wired into `make_queue_decider`; autonomous path already passes it through); `reconcile` gained a misfire net; and `_maybe_enqueue_merge` was made **dedup-aware** (queued-OR-already-queued = handoff; fails only on an enqueue exception) — which review-reliability caught also closes a pre-existing dedup→early-`Done` (R19) gap. §4 example updated. spec-compliance + reliability reviews SATISFIED; regression tests added. | Major | 2026-06-23 | LIN-27 `--worker claude` production dogfood (full watched daemon loop) | fixed (`c081eac`+`f8fb6c9`; reviewed) |
| **Worker context-budget — accepted residuals + proposed rule (completion gate):** (a) commit-detection in `worker-runtime/harness/src/context/budgetHook.ts` (`isGitCommit`/`bashFailed`) is intentionally **best-effort** — a contrived `fatal:` in a commit's stdout, or an unusual git invocation, could mis-read; harmless by design (a false checkpoint only injects an advisory usage nudge gated on ≥soft, never forces a compaction — plan assumption #3). (b) The budget feature is wired only on the **app-server `openSession` path**, not the `cc-harness` daemon (`DaemonSession` accepts only `{contextTool,compactTool}`) — acceptable for an app-server-targeted feature; wire `contextBudget` into the daemon if a daemon-hosted worker ever needs it. (c) Proposed harness module convention (review-arch): document that option-shaping helpers split by verb — `apply*Persona` MUTATES `options` in place, `with*Tool`/`with*Hooks` return a COPY — currently tribal knowledge faithfully followed by `applyContextBudgetPersona` vs `withContextBudgetHooks`. | Minor | 2026-06-24 | worker-context-budget completion gate (review-arch + review-reliability P2 / proposed rule) | open (a,b accepted; c doc-debt) |
| **Worker self-compaction does not actually FIRE — the budget persona/push is too soft to drive the behavior.** Keyed live runs (haiku + sonnet) proved the wiring is sound (a sentinel diagnostic confirmed the `UserPromptSubmit` push reaches the model on a programmatic `submit()`; the tool is callable) but the model declines to call `RequestCompaction` from the standing persona alone — it trusts its real "~30k is tiny for my window" read over the configured thresholds, reinforced by the persona's own "you have a large context window; do not compact reflexively." Shipped as **advisory best-effort** by human decision (native SDK auto-compact is the floor). Lever to make it fire if wanted: (1) drop the "large window / don't compact reflexively" reassurance, (2) tell the model to TRUST the configured budget over its room-sense, (3) make the past-target checkpoint push imperative ("past your budget at a checkpoint — RequestCompaction now") while keeping the work-preservation guard; then re-validate live on the worker's real tier. A deterministic runtime-triggered compaction at the hard ceiling is the stronger fallback (crosses "model decides"). | Minor | 2026-06-24 | worker-context-budget behavioral check (keyed live, haiku+sonnet) | open (advisory shipped; strengthen lever deferred by human decision) |
